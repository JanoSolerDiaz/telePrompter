# SEGUIMIENTO — teleprompter — Hub / panel de control

> Hub del registro repartido (ver §0.4 de `HOJA_DE_RUTA.md`). Aquí viven el estado y lo
> transversal; el detalle vive en los documentos vivos de `roadmap/`.
> El dueño no revisa el código: revisa este documento.
>
> **Documentos hermanos:** las **decisiones técnicas** están en `DECISIONES_TECNICAS.md`
> (antiguo §2) y la **bitácora de sesiones** en `HISTORIAL_SESIONES.md` (antiguo §8). Las
> secciones no se renumeran para no romper referencias.

**Hoja de ruta de referencia:** `HOJA_DE_RUTA.md` v1.3 (2026-08-31)
**Modo de operación:** AUTONOMÍA TOTAL
**Última actualización:** 2026-09-03 — **R-02 (registro de tomas por escena) COMPLETADA**, sesión de nube, segunda de oleada v2. Sin hallazgos ABIERTOS de severidad alta en `auditoriacontinua.md` al empezar, así que se procedió directo con la cola normal (R-02, siguiente tras R-01 según §1). El índice deja de ser solo una lista de estado (T-19) y pasa a ser el parte de rodaje real, sobre `assets/reproductor/guion.js`/`estilo.css` con una pieza nueva del lado Python (`scripts/tomas.py`) para el requisito 3: (1) **número de tomas, duración real y toma buena** (requisito 1) — una toma se cierra sola (`finalizarTomaActual`, mismo cronómetro de T-23, congelado en pausa) al volver al índice, al cambiar de escena con las flechas sin pasar por el índice, o al reiniciar la escena en curso (`R`) — reiniciar pasa a significar "esta toma no vale, repito": cierra la que se abandona y arranca el cronómetro de cero para la siguiente, en vez de heredarle tiempo; como mucho una toma por escena queda marcada como la buena, marcar una nueva desmarca la anterior. (2) **Nota rápida sin salir de grabación, con el mínimo de teclas** (requisito 2) — dos teclas nuevas en el mapa ya configurable de T-24 (`G` marca la toma en curso como buena, `N` abre un `window.prompt()` para la nota), ningún campo nuevo en `Configuracion` porque solo son entradas nuevas de la tupla `mapa_teclas_reproductor` que ya existía. (3) **Volcado a un archivo, legible por el dueño y por la fase de montaje** (requisito 3) — el botón "Exportar parte de rodaje" descarga un `.json` (mismo patrón de descarga que "Exportar preferencias" de R-01, duplicado a propósito por ser datos de evolución independiente); `scripts/tomas.py` (nuevo) valida ese archivo y lo fusiona en `estado.json` con un contenedor `tomas` nuevo (migración `002_tomas.py`, `VERSION_ESQUEMA_ESTADO` sube de 1 a 2), reemplazando por escena con lo más reciente exportado y sin borrar nunca las escenas que una exportación concreta no menciona; contrato completo en `references/contrato-tomas.md` (nuevo, mismo tratamiento que `contrato-tarjetas.md`/`contrato-montaje.md`). (4) **El índice muestra de un vistazo qué falta, qué se repitió y qué está resuelto** (requisito 4) — el estado por escena (Pendiente/Grabada/Revisada) ya no es un flip manual al primer visitar (como hacía T-19): se deriva de las tomas reales (sin tomas / con tomas sin ninguna buena / con una toma buena), y un resumen `"N tomas · buena: X"` se añade junto a cada escena. **Hallazgo propio corregido en la misma sesión, sin abrir P-XX por ser parte directa del propio requisito 4** ("el índice muestra de un vistazo"): "Restablecer preferencias" (T-26) borra toda clave de `localStorage` con el prefijo del guion, lo que habría incluido el registro de tomas por compartir espacio de nombres pese a no ser una preferencia — corregido excluyendo el prefijo `tomas_escena_` de esa limpieza antes de que llegara a pasar de verdad. Verificado de punta a punta con Playwright headless (Chromium, herramienta de esta sesión, nunca dependencia del proyecto) sobre un reproductor real generado de `fixtures/guion-ejemplo.md`: dos tomas en la escena 1 (la segunda marcada buena tras reiniciar), una toma sin marcar en la escena 2, resumen y badge "Revisada" correctos en el índice, exportación produce un `.json` válido, y el registro sobrevive a cerrar y reabrir el MISMO perfil de navegador (cierre real, no una recarga) — el `.json` descargado se volvió a cargar y fusionar de verdad con `scripts/tomas.py` sobre un `estado.json` real, cerrando el contrato de punta a punta. Suite: 444 pasan + 0 skipped (antes 415; 29 tests nuevos: 15 en `tests/test_tomas.py`, 5 en `tests/test_migraciones.py` para la migración 002, 9 en `tests/test_reproductor.py`). Las cuatro verificaciones en verde. **Siguiente tarea: R-03 (marcar tropiezos durante la toma), tercera de oleada v2.**
>
> **Sesión anterior (2026-09-03) — T-32 COMPLETADA (desbloqueada), P-04 y P-05 COMPLETADAS**, sesión local en la máquina del dueño. **P-05 es la más urgente de las tres y salió de usar la skill de verdad, no de leer código:** al reinstalarla, la copia de seguridad que hace `instalar_skill.py` quedaba DENTRO de `~/.claude/skills/`, y Claude Code registra como skill toda subcarpeta de ahí con un `SKILL.md` — así que `teleprompter.bak-<marca>` apareció en la lista como una **segunda `teleprompter` idéntica**, compitiendo con la real, y cada reinstalación añadiría otra. Corregido: las copias van a `~/.claude/teleprompter-copias-de-seguridad/`. La copia de hoy se movió a mano. Primer recordatorio de que el health check verde no cubre la integración con el entorno de Claude Code. (1) **T-32 se desbloquea por fin:** el dueño ejecutó la instalación real y el health check desde la copia instalada dio **11/11 etapas OK**, así que T-32 pasa de BLOQUEADA a COMPLETADA y el bloqueo #6 de §3 queda RESUELTO. **Con esto la oleada v1 tiene todo su código entregado y verificado en destino**; solo siguen abiertos, sin urgencia, el bloqueo #2 (paquete `480-branded-pptx`, que mantiene latente la salida `.pptx`) y el #5 (clicker, espera indefinida). Dato nuevo del entorno real: en la máquina del dueño **el `.pdf` sí se genera de verdad** (tiene Chrome/Edge), a diferencia de toda verificación anterior, hecha en contenedores sin navegador. (2) **P-04, autopropuesta:** esta sesión diagnosticó el `#14` en paralelo y de forma independiente a la sesión de nube que lo cerró como P-03 — mismo análisis de causa raíz y misma clave `particiones_pospuestas`, lo que refuerza que el diagnóstico es correcto. Al traer el remoto se descartó el arreglo duplicado y se conservó solo lo que P-03 no cubría: lectura tolerante de la disposición persistida (antes, basura en `estado.validacion` tiraba la revalidación entera), persistir solo las particiones realmente pospuestas en vez de todo bloque editado, **incidencia nueva cuando las anclas del documento no son las previstas** — sin ella, un `estado.json` anterior a P-03 en mitad de un conflicto reproduce el `#14` en silencio — y corregir el mensaje del conflicto, que prometía una materialización que nunca ocurre. 5 tests nuevos; verificado que los 3 que cubren comportamiento nuevo fallan sin el código de P-04. (3) **Aviso para la próxima sesión:** los 4 tests en rojo de esta sesión son de **plataforma Windows**, no regresiones (fallan igual sobre el remoto sin tocar nada, y en nube la suite va limpia). Uno destapa un problema real de producto: `entrada.leer_guion` no normaliza `\r\n`, y los guiones del dueño en Windows serán CRLF. Queda propuesto como próxima P-XX; ver la entrada de sesión.
>
> **Sesión anterior (2026-09-03) — R-01 (persistencia verificada + plan B) COMPLETADA**, primera de oleada v2, sesión de nube. Sin hallazgos ABIERTOS de severidad alta en `auditoriacontinua.md` al empezar (`#14` cerrado por P-03 en la sesión anterior), así que se procedió directo con la cola normal. Ver el detalle en la entrada de sesión de abajo y en `DECISIONES_TECNICAS.md`.
>
> **Sesión de código (2026-09-03) — R-01 (persistencia verificada + plan B) COMPLETADA**, primera de oleada v2 (`origen: auditoría #5`), sesión de nube. Sin hallazgos ABIERTOS de severidad alta en `auditoriacontinua.md` al empezar, así que se procedió directo con la cola normal. La ficha exige "comprobación real en el navegador de grabación", algo que en principio suena al mismo tipo de bloqueo que T-32 (instalación en la máquina del dueño) — pero a diferencia de una ruta específica del sistema del dueño, el reproductor es una página HTML corriente que cualquier navegador real puede abrir y cerrar de verdad: se instaló Playwright (solo como herramienta de verificación de esta sesión, nunca como dependencia del proyecto) y se usó el Chromium ya preinstalado en el contenedor de nube para reproducir el escenario exacto del requisito 1 con `launch_persistent_context` — guardar preferencias, cerrar el contexto (cierre real del navegador, no una recarga), reabrir el MISMO perfil de datos de usuario: las preferencias sobreviven intactas; con un perfil nuevo (otro navegador/usuario/datos borrados), `localStorage` aparece vacío, el comportamiento esperado de un almacenamiento particionado por origen. Resultado documentado en `DECISIONES_TECNICAS.md` en vez de asumido, con la salvedad honesta de que el navegador de grabación real del dueño puede no ser exactamente este. Entero en `assets/reproductor/guion.js`/`estilo.css`, sin tocar ningún archivo Python: (1) `comprobarAlmacenamientoDisponible()` escribe y relee una clave de prueba al cargar la página — detecta con certeza si `localStorage` no funciona en absoluto (requisito 1, complementa la verificación de arriba) y dispara un aviso `.aviso-almacenamiento` visible en el índice en vez del silencio anterior de T-25/T-26 (requisito 3); (2) dos botones nuevos en el índice, "Exportar preferencias" e "Importar preferencias" (requisito 2): la exportación arma un `.json` (tamaño, velocidad por escena, espejo, indicadores, última escena vista) leyendo siempre de las variables en memoria — nunca de `localStorage` en el momento del clic, para que funcione igual de bien cuando el almacenamiento está completamente bloqueado, verificado a propósito — y dispara una descarga real (`Blob`+`URL.createObjectURL`, confirmado que funciona desde `file://` en Chromium) con `window.prompt()` como red de seguridad si esa vía fallara; la importación lee el archivo elegido, rechaza con un mensaje claro las preferencias de otro guión, y aplica cada campo reconocido acotado a los mismos límites que sus ajustes en vivo. Verificado de punta a punta con Playwright sobre `fixtures/salida/reproductor.html` real: exportar desde una sesión con ajustes cambiados, importar ese archivo en un perfil de navegador completamente nuevo (sin nada guardado) hace aparecer el botón "Continuar" con la escena/bloque correctos y puebla `localStorage` del nuevo perfil; con `localStorage` bloqueado del todo, el aviso aparece, el reproductor sigue funcionando con normalidad y "Exportar preferencias" sigue produciendo un `.json` completo. Suite: 409 pasan + 0 skipped (antes 404; 5 tests nuevos en `tests/test_reproductor.py`). Las cuatro verificaciones en verde. **Siguiente tarea: R-02 (registro de tomas por escena), segunda de oleada v2.**
>
> **Sesión de código (2026-09-03) — P-03 (hallazgo #14, severidad alta) COMPLETADA**, atendida como urgente antes de R-01 según §0.1/§0.3. El hallazgo, de la auditoría del mismo día: el límite que P-02 (2026-09-02) había dejado documentado como "un fragmento de texto de origen sin editar" en una revalidación posterior a un conflicto edición/partición pospuesto resultó, verificado con reproducción de código independiente por el auditor, mucho más grave — duplicación real de contenido en `guion-escenas.md` (una escena de 2 bloques pasaba a 3, con el bloque siguiente repetido y mal atribuido) sin ningún aviso que lo señalara. Causa raíz: al posponer una partición por conflicto (P-02), la decisión de posponerla no se conservaba entre pasadas — la pasada SIGUIENTE recalculaba el esquema de anclas (`identidad_por_ancla`) asumiendo que toda partición aceptada ya estaba materializada, mientras el documento en disco seguía teniendo el esquema sin partir de la pasada anterior; el desajuste entre ambos esquemas es lo que atribuía mal el contenido. Corregido en `scripts/revalidacion.py`: `_particiones_pospuestas_previas`/`_guardar_particiones_pospuestas` persisten en `estado.validacion["particiones_pospuestas"]` (contenedor genérico ya reservado desde T-07, sin migración) el conjunto de bloques pospuestos al final de cada pasada, y la pasada siguiente lo usa para calcular el esquema de anclas con el que interpreta el documento — así siempre coincide con el que tenía cuando se escribió. Efecto (deliberado, ver `DECISIONES_TECNICAS.md`): mientras la edición manual del dueño siga en el documento, la partición queda pospuesta indefinidamente sin duplicar ni mal atribuir nada; solo se materializa cuando el dueño deja de tocar el bloque (su texto vuelve a coincidir con el que el sistema derivaría). Dos tests nuevos en `tests/test_revalidacion.py` (`test_revalidacion_posterior_al_conflicto_no_duplica_bloques`, que reproduce el escenario exacto de #14 y falla sin el fix — verificado con `git stash` antes de dar la corrección por buena —, y `test_particion_pospuesta_se_materializa_cuando_se_retira_la_edicion`, que confirma que la materialización sigue funcionando con normalidad cuando el conflicto se resuelve). Suite: 404 pasan + 0 skipped (antes 402; 2 tests nuevos). Las cuatro verificaciones en verde. `DEVELOPERS.md` y `auditoriacontinua.md` actualizados con el cierre. **No queda ninguna T-XX PENDIENTE en §1: la próxima sesión de código retoma la cola normal en R-01 (oleada v2), primera de `ROADMAP_PRODUCTO.md`.**
>
> **Sesión anterior (2026-09-02) — Ciclo de Product Manager** (sin sesión de código). No hay ninguna T-XX PENDIENTE en §1 (oleada v1 entregada salvo los dos bloqueos del dueño, T-32 y T-24b, que no frenan nada); la cola de producto está en R-01, oleada v2. Revisado `auditoriacontinua.md`: sin hallazgos ABIERTOS de severidad alta (el #9 sigue cerrado en código por P-02, pendiente solo de reevaluación del auditor). De los hallazgos ABIERTOS de severidad media/baja, **#5 y #6 ya estaban enrutados** a R-01 y R-06 respectivamente (sin cambios); **#10, #11 y #12 (números mágicos del índice del reproductor, `PROYECTO.md` desactualizado, desajuste de versión de Python) se agrupan en `R-08` nueva**, y **#13 (huecos del validador de auto-contención: `<object>`/`<base href>`/`WebSocket`/`EventSource`/`sendBeacon`/`url(...)` CSS) pasa a `R-09` nueva** — ambas en Fase F-D de `ROADMAP_PRODUCTO.md`, PENDIENTES en §1. `FEEDBACK.md` no tiene entradas `nuevo` reales todavía (solo la fila plantilla vacía): no hay feedback de rodaje que incorporar en este ciclo, algo esperable porque el criterio de salida de la oleada v1 —grabar un curso entero con la skill— todavía no se ha cumplido. Revisado el resto del roadmap de producto (R-01 a R-07): sigue coherente con lo entregado en T-19 a T-33 (en particular con `references/contrato-montaje.md` de T-33, que ya anticipaba R-02/R-04/R-05 sin adelantarlas), así que no hace falta reescribir ninguna ficha existente. Ninguna oleada está 100 % entregada todavía (oleada v1 no cierra hasta que se grabe un curso real, criterio que también conmuta el modo a PRODUCCIÓN), así que no se mueve nada a `ROADMAP_HISTORICO.md` en este ciclo. Sin preguntas nuevas para el dueño: R-08 y R-09 son deuda de mantenimiento sin decisión de producto pendiente.
>
> **Sesión anterior (2026-09-02) — T-33 (encaje con la cadena de montaje de vídeo) COMPLETADA** por una sesión de nube, tercera de FASE B6 (cierra el backlog de T-XX conocido; solo quedan pendientes las R-XX de producto). Sin hallazgos ABIERTOS de severidad alta en `auditoriacontinua.md` al empezar (el #9 sigue cerrado en código por P-02, solo pendiente de que el auditor lo reevalúe), así que se procedió directo con la cola normal. T-33 depende de T-27 (COMPLETADA) y T-32 (BLOQUEADA solo en su instalación real): sus propios requisitos no necesitaban esa instalación, solo los artefactos que T-32 ya entregó, tal como preveía la decisión de la sesión anterior. Tarea de contrato y verificación, sin salida nueva: (1) `references/contrato-montaje.md` (nuevo, requisitos 1 y 4) documenta la carpeta y los nombres de archivo de salida, que la fase de montaje solo necesita leer `guion.srt` y `tarjetas.json`, y la fórmula para derivar el instante de inicio/fin de cada escena sumando `duracion_estimada_segundos` en orden — sin añadir un `inicio_segundos`/`fin_segundos` absoluto nuevo al contrato de `tarjetas.json`, porque ya es derivable sin ambigüedad de lo que T-29 entregó; (2) `SKILL.md` gana la sección «Donde encaja: la cadena de montaje de vídeo (T-33)» (requisito 4), sustituyendo el stub que ya apuntaba a esta tarea; (3) requisito 2 («nombres y orden de escenas estables y predecibles»): hasta ahora `parser.py` aceptaba el número de escena del encabezado (`## BLOQUE N — <título>`) sin comprobar que fuera único ni creciente — un guion con dos escenas `BLOQUE 2` habría producido en silencio dos tarjetas con el mismo número en `tarjetas.json`. `convencion.detectar_desviaciones` (T-10) gana dos tipos nuevos, `numero_escena_duplicado` y `numero_escena_no_creciente`, que señalan el problema sin bloquear el proceso (mismo patrón que el resto de esa función); los tres guiones reales y el guion de ejemplo (T-32) ya numeran sus escenas sin huecos ni repeticiones, así que no cambia ningún comportamiento sobre material real, solo dejan de pasar desapercibidos en un guion hostil; (4) `tests/test_integracion_montaje.py` (nuevo, requisito 3) es el primer test que genera `.srt` y `tarjetas.json` a partir del MISMO `ResultadoTiempos` de cada guion real y comprueba que son consistentes entre sí — no solo que cada uno pasa su propio validador por separado, que ya cubrían `test_srt.py`/`test_pptx.py` —: cero desviaciones de numeración de escena, el `.srt` se valida sin avisos (criterio de aceptación literal), `tarjetas.json` cumple su esquema, el fin del último subtítulo coincide exactamente con `duracion_total_segundos`, y el orden de `numero` es el mismo en ambas salidas y en el guion de origen. Suite: 402 pasan + 0 skipped (antes 399; 3 tests nuevos: 2 en `tests/test_convencion.py`, 1 en `tests/test_integracion_montaje.py`). Las cuatro verificaciones en verde, incluida `verificar_salidas.py --fixture` con las etapas "Generación del .srt"/"Validez del .srt"/"Generación de tarjetas.json y brief (.pptx)"/"Validez de tarjetas.json" en OK sobre `fixtures/guion-ejemplo.md`. **No queda ninguna T-XX PENDIENTE en §1: la cola normal pasa a la primera R-XX (`R-01`, oleada v2) en la próxima sesión, salvo que el dueño reordene la prioridad.**
>
> **Sesión anterior (2026-09-02) — T-32 (instalación de la skill y guion de ejemplo) BLOQUEADA solo en su último tramo** por una sesión de nube, segunda de FASE B6. Sin hallazgos ABIERTOS de severidad alta en `auditoriacontinua.md` al empezar (el #9 sigue cerrado en código por P-02, solo pendiente de que el auditor lo reevalúe), así que se procedió directo con la cola normal. Entregadas desde la nube las tres partes que no dependen de la máquina real del dueño (nota de entorno, protocolo v1.3): (1) `scripts/instalar_skill.py` (nuevo) — `sincronizar_skill` copia el paquete distribuible (`SKILL.md`, `scripts/`, `assets/`, `references/`, `fixtures/guion-ejemplo.md`; deja fuera `tests/`, `roadmap/`, `.github/`, `DEVELOPERS.md`) al destino (`--destino`, por defecto `config.RUTA_INSTALACION_SKILL`), haciendo copia de seguridad completa (`<nombre>.bak-<marca>`) de cualquier instalación previa antes de reinstalar desde cero — nunca una sincronización incremental que tuviera que decidir qué borrar (invariante (d), §0.2); (2) `fixtures/guion-ejemplo.md` (requisito 2) — guion de curso sintético con locución, `**EN PANTALLA**`, una `**NOTA**` interna, B-roll y timestamps, calibrado a propósito para que su ritmo no dispare el aviso de desviación de T-12 (ese aviso ya lo prueba `tests/test_tiempos.py`; este fixture enseña el formato de salida) — y `fixtures/guion-ejemplo-esperado.md`, su versión anotada esperada, con un test de regresión byte a byte (`tests/test_fixture_ejemplo.py`); (3) `DEVELOPERS.md` gana «Instalación de la skill y guion de ejemplo (T-32)» con dos subsecciones nuevas que la tarea pedía explícitamente y que no existían todavía — «Arquitectura y mapa de módulos» (el pipeline completo, módulo a módulo, en el orden real de dependencia) y «Cómo añadir una regla nueva» (las tres recetas: normalización, detección/aviso, clasificación). El requisito 3 (health check ejecutable desde la copia instalada) se verificó de verdad pero contra un `tmp_path` sincronizado, nunca contra la ruta real: `tests/test_instalar_skill.py::test_health_check_funciona_ejecutado_desde_la_copia_instalada` sincroniza una copia y ejecuta `verificar_salidas.py --fixture` como subproceso aislado sobre ella, en verde. `verificar_salidas.py` pasa a usar `fixtures/guion-ejemplo.md` como guion de sus etapas de generación (`_ruta_guion_para_verificar`, con el guion real como respaldo): la etapa "Guion de ejemplo" deja de ser NO APLICABLE. Suite: 399 pasan + 0 skipped (antes 390; 9 tests nuevos: 4 en `tests/test_fixture_ejemplo.py`, 5 en `tests/test_instalar_skill.py`). Las cuatro verificaciones en verde. **La tarea NO se marca COMPLETADA: el criterio de aceptación literal («tras instalar, el health check pasa desde la ruta instalada») exige una instalación real en `~/.claude/skills/teleprompter/`, que solo el dueño o una sesión local pueden ejecutar y confirmar — hacerlo aquí no sería una instalación, solo su apariencia. Ver bloqueo #6 de §3. T-33 NO queda bloqueada en cascada: sus propios requisitos no necesitan la instalación real, solo los artefactos que T-32 ya entregó. Siguiente tarea: T-33 (encaje con la cadena de montaje de vídeo).**
>
> **Sesión anterior (2026-09-02) — T-31 (`SKILL.md` y configuración completa) COMPLETADA** por una sesión de nube, primera de FASE B6. Sin hallazgos ABIERTOS de severidad alta en `auditoriacontinua.md` al empezar (el #9 sigue cerrado en código por P-02, solo pendiente de que el auditor lo reevalúe y lo marque RESUELTO en su propio documento), así que se procedió directo con la cola normal. Tarea puramente documental: ningún script de `scripts/` cambia de comportamiento. (1) `SKILL.md` gana la sección «Precedencia de configuración (T-31)» que explica los cuatro niveles (por defecto → usuario → proyecto de guion → invocación) y el mecanismo real hoy — esta skill no tiene ni tendrá una CLI propia (decisión ya sentada en T-30), así que "argumentos de la invocación" es Claude construyendo `Configuracion(**overrides)` dentro de la sesión, y "configuración del proyecto de guion" ya la cumple `configuracion_efectiva` en `estado.json` (T-07) sin necesitar nada nuevo; (2) sustituye el «extracto» del final por «Valores por defecto — tabla completa», con los 81 campos de `Configuracion` agrupados por área (ritmo y tiempos, troceo, convención, detector de problemas, documento de revisión, reproductor/tema, autoscroll, ayudas de grabación, atajos y clicker, espejo, `.srt`, `.pdf`, `.pptx`); (3) `tests/test_skill_md.py` (4 tests nuevos) es el requisito literal de la tarea — compara `dataclasses.fields(Configuracion)` contra las claves citadas entre backticks en esa sección, en las dos direcciones, delimitando la sección por su propio encabezado (`_PATRON_SIGUIENTE_H2` evita que un `### ` de subsección se confunda con el siguiente `## ` real); las tablas de sustitución de T-13/T-14 (`SIMBOLOS_MONEDA`, `UNIDADES_ABREVIADAS`, `ANGLICISMOS_COMUNES`) y `PATRON_ENCABEZADO_ESCENA` quedan fuera de la comparación por decisión ya tomada (no son campos de `Configuracion`), documentadas por nombre pero fuera de esta tabla; (4) documentación extensa (requisito 4) movida a tres archivos nuevos en `references/` — `convencion-guion.md`, `formato-guion-escenas.md`, `mapa-teclas.md` — con un enlace «Ver también» desde la sección correspondiente de `SKILL.md`, que conserva sus tablas y resúmenes cortos ya existentes sin recortarlos; (5) `DEVELOPERS.md` gana la sección T-31 explicando el test de completitud para que quien añada un campo nuevo a `Configuracion` sepa que tiene que documentarlo en el mismo commit. Se retira la nota «BORRADOR (T-00)» de la cabecera de `SKILL.md`, ya cumplida. Suite: 390 pasan + 0 skipped (antes 386; 4 tests nuevos en `tests/test_skill_md.py`). Las cuatro verificaciones en verde. **FASE B6 (empaquetado como skill e integración) en curso.**
>
> **Sesión anterior (2026-09-02) — T-30 (selector de salidas por validación) COMPLETADA** por una sesión de nube, cuarta y última de FASE B5 (FASE B5 se cierra con esta tarea). Sin hallazgos ABIERTOS de severidad alta en `auditoriacontinua.md` al empezar (el #9 sigue cerrado en código por P-02, solo pendiente de que el auditor lo reevalúe y lo marque RESUELTO en su propio documento), así que se procedió directo con la cola normal. `scripts/salidas.py` nuevo: ata en una sola canalización los cuatro generadores ya completos (`reproductor.py` T-18, `srt.py` T-27, `pdf.py` T-28, `pptx.py` T-29) sin duplicar nada de lo que cada uno ya hace. (1) `construir_pregunta_salidas` arma la pregunta de opción múltiple (requisito 1) como datos (`PreguntaSeleccionSalidas`/`OpcionSalida`), no como un `input()` de terminal — esta skill no tiene ni tendrá una CLI interactiva propia, es Claude quien la formula al dueño dentro de la sesión, mismo patrón que `parser.DeteccionEscenasAmbiguaError` (T-08); (2) la sugerencia (requisito 2) lee la última entrada de `estado.salidas_generadas` (contenedor genérico reservado desde T-07, sin migración nueva — el enunciado fija literalmente "Migración: No") y nunca decide en silencio: sin histórico, sugiere las cuatro; (3) `generar_salidas_seleccionadas` genera cada tipo seleccionado con un `try`/`except` propio (requisito 3): el fallo real de una nunca impide las demás, y la latencia que ya devuelven `pdf.exportar_pdf` y `pptx.exportar_pptx` se refleja como `SalidaLatente` sin confundirla con un fallo — el HTML de impresión y `tarjetas.json`/el brief son archivos reales en disco aunque el `.pdf`/`.pptx` final quede pendiente; (4) `ResumenSalidas` (requisito 4) trae tres listas — generadas (ruta y tamaño ya leído de disco), omitidas y latentes, ambas con motivo — más `como_dict()` para `registrar_generacion` (append-only sobre `estado.salidas_generadas`) y `mostrar_resumen()` para pintarlo por `presentacion.py`. `verificar_salidas.py` gana la etapa "Generación de salidas" (antes NO APLICABLE desde T-00, hallazgo #4) que ejecuta la canalización completa con las cuatro salidas seleccionadas sobre el mismo guion real que usan las demás etapas — en esta máquina de verificación queda en OK con el `.pptx` latente (skill de marca ausente) y el `.pdf` real latente (sin Chrome/Edge), sin fallar por ninguna de las dos. `estado._marca_de_tiempo` pasa de privada a pública (`estado.marca_de_tiempo`, mismo patrón que `tiempos.PAUSA_FIN_ESCENA` en T-27) porque `salidas.py` es su segundo consumidor. Suite: 386 pasan + 0 skipped (antes 377; 9 tests nuevos en `tests/test_salidas.py`, incluido el criterio de aceptación literal —con el `.pptx` latente, las otras tres se generan igualmente y el resumen lo refleja— y que dos validaciones seguidas sobre el mismo `estado.json` en disco preguntan las dos veces, cada una sugiriendo la selección de la pasada anterior). Las cuatro verificaciones en verde. **FASE B5 (exportadores adicionales) queda cerrada. Siguiente tarea: T-31 (`SKILL.md` y configuración completa), primera de FASE B6.**
>
> **Sesión anterior (2026-09-02) — T-29 (adaptador `.pptx`, delegación en `480-branded-pptx`) COMPLETADA** por una sesión de nube, tercera de FASE B5. Sin hallazgos ABIERTOS de severidad alta en `auditoriacontinua.md` al empezar (el #9 sigue cerrado en código por P-02, solo pendiente de que el auditor lo reevalúe y lo marque RESUELTO en su propio documento), así que se procedió directo con la cola normal. `scripts/pptx.py` nuevo: esta skill **no genera el `.pptx` por su cuenta** (verificado con el `SKILL.md` de `480-branded-pptx`: son instrucciones para Claude, no un ejecutable) — produce `tarjetas.json` (contrato de intercambio, documentado en `references/contrato-tarjetas.md`, requisito 1) y `brief-pptx.md` (brief de invocación en Markdown, requisito 2), y es Claude quien genera el `.pptx` de verdad delegando en esa skill dentro de la misma sesión. Reutiliza tres funciones de `pdf.py` (T-28) promovidas de privadas a públicas en esta tarea (`dimensiones_png`, `es_nota_interna`, `indicaciones_no_recitables`, mismo patrón que `tiempos.PAUSA_FIN_ESCENA` en T-27) para no duplicar ni el criterio de nota interna/indicación de pantalla ni la medición del logotipo. El modo `--para-terceros` (mismo `incluir_notas_internas` de T-28, requisito 3) vacía `notas_internas` en el propio `tarjetas.json`, no solo en la presentación — decisión deliberada porque el contrato es independiente del generador. El brief corrige por escrito las dos discrepancias conocidas del `SKILL.md` de la skill de marca con los assets reales de este proyecto (requisito 2): Poppins en vez de Figtree, y la relación de aspecto medida del PNG (con tabla de alturas ya calculada) en vez de la constante `668/376`. Estructura de deck configurable: `pptx_escenas_por_diapositiva` (1 por defecto, el que exige el criterio de aceptación literal) y `pptx_umbral_indice_secciones` (4, de `references/marca-480.md`). `detectar_skill_pptx_disponible` (requisito 4) comprueba solo que `~/.claude/skills/480-branded-pptx` y `~/.claude/skills/pptx` existen como carpetas; ausentes en esta sesión de nube, así que `tarjetas.json` y el brief se generan igual y la salida `.pptx` queda LATENTE sin que la generación falle nunca — verificado con `verificar_salidas.py --fixture` sobre el mismo guion real que usan el reproductor, el `.srt` y el HTML de impresión (dos etapas nuevas, "Generación de tarjetas.json y brief (.pptx)" y "Validez de tarjetas.json", sustituyen el silencio anterior sobre esta salida). Suite: 377 pasan + 0 skipped (antes 354; 23 tests nuevos en `tests/test_pptx.py`, incluido el criterio de aceptación literal —tantas diapositivas de contenido como escenas— sobre un guion de prueba y sobre los tres guiones reales). Las cuatro verificaciones en verde.
>
> **Sesión anterior (2026-09-02) — T-28 (exportador `.pdf` con identidad 480) COMPLETADA** por una sesión de nube, segunda de FASE B5. Sin hallazgos ABIERTOS de severidad alta en `auditoriacontinua.md` al empezar (el #9 sigue cerrado en código por P-02, solo pendiente de que el auditor lo reevalúe y lo marque RESUELTO en su propio documento), así que se procedió directo con la cola normal. `scripts/pdf.py` nuevo, con plantillas en `assets/pdf/` (mismo patrón que el reproductor, T-18): (1) HTML de impresión con `@page`/saltos de página por escena siguiendo `references/marca-480.md` (fondo blanco, paleta 480, interlineado 1,4, alineación izquierda, línea de acento verde bajo los títulos); (2) tipografía Poppins con respaldo Montserrat → Calibri → sans del sistema (`Configuracion.respaldo_tipografico`, constante reservada desde antes de esta tarea, ahora cableada al dataclass); (3) logotipo (variante Gris) incrustado como `data:image/png;base64,...` — nunca una ruta relativa, porque el validador de auto-contención rechaza cualquier `src=` que no sea `data:`, ni siquiera un archivo local del propio proyecto —, con la relación de aspecto medida en tiempo de generación leyendo la cabecera `IHDR` del PNG (1993×805, ratio 2,4758 verificado; la constante `668/376` de la guía de marca no se usa en ningún sitio), ausente sin romper la generación; (4) conversión con Chrome/Edge en modo headless (`--print-to-pdf`), detectado por nombre conocido o rutas de instalación estándar de Windows/macOS (`pdf_chrome_ejecutable_manual` para fijarlo a mano); **verificado en esta sesión** que Chrome se niega a arrancar como root sin `--no-sandbox` (sandbox de nube), así que ese flag se añade solo cuando `os.geteuid() == 0`; sin Chrome disponible, deja el HTML de impresión listo con instrucciones de Ctrl+P, sin fallar nunca; (5) una escena por página con duración objetivo/estimada y el texto de locución como **prosa continua** (bloques separados por un punto medio discreto, nunca una lista), indicaciones no recitables al pie; (6) modo `--para-terceros` vía `Configuracion.incluir_notas_internas=False` (el mismo interruptor que reutilizará T-29): omite las indicaciones marcadas como nota interna (rótulo `NOTA`, detectado por el motivo de clasificación de T-09) y mantiene siempre las indicaciones de pantalla y las ambiguas sin señal clara — nunca se decide en silencio que algo es prescindible; este módulo, a diferencia de `guion-escenas.md` (T-16), no muestra en ningún modo el aparato de reescrituras, ver `DECISIONES_TECNICAS.md`; (7) portada con título, duración objetivo/total, escenas y palabras. `verificar_salidas.py` gana dos etapas nuevas ("Generación del HTML de impresión (.pdf)", "Auto-contención del HTML de impresión") que sustituyen el `NO APLICABLE` fijo desde T-00, generando de verdad sobre el mismo guion real que usan el reproductor y el `.srt`. Verificado a mano con el Chromium de este entorno (no es dependencia del proyecto, solo para validar en esta sesión): PDF real de 8 páginas sobre un guion de 7 escenas (escenas + portada), tamaño Carta (612×792 pt) con márgenes del `@page`, sin ningún recurso externo. Suite: 354 pasan + 0 skipped (antes 337; 17 tests nuevos en `tests/test_pdf.py`, incluidos dos que generan un `.pdf` real de verdad cuando hay Chrome disponible y se saltan si no — mismo patrón de degradación elegante que el resto del proyecto ante hardware/software ausente). Las cuatro verificaciones en verde. **Siguiente tarea: T-29 (adaptador `.pptx`), tercera de FASE B5.**
>
> **Sesión anterior (2026-09-02) — T-27 (exportador `.srt` borrador) COMPLETADA** por una sesión de nube, primera de FASE B5. Sin hallazgos ABIERTOS de severidad alta en `auditoriacontinua.md` al empezar (el #9 sigue cerrado en código por P-02, pendiente solo de que el auditor lo reevalúe), así que se procedió directo con la cola normal. `scripts/srt.py` nuevo: (1) un subtítulo por bloque de respiración con los tiempos ya calculados por T-12 (`ResultadoTiempos.bloques`, ninguna fuente de tiempos propia); (2) sobre el **texto locutado final** — recibe el `ResultadoTiempos` tal cual se lo pasan, así que consumido desde `revalidacion.revalidar_guion().resultado_tiempos` ya trae las reescrituras aceptadas materializadas (requisito 4), sin volver a leer `estado.json` por su cuenta; (3) un bloque cuya duración total (palabras + pausa) no llega a `Configuracion.srt_duracion_minima_segundos` (1.2 s por defecto) se funde con el siguiente de la misma escena, nunca cruzando un fin de escena — reconocido por `tiempos.PAUSA_FIN_ESCENA`, promovida de privada a pública en esta tarea porque es parte del contrato de `BloqueConTiempo.tipo_pausa` y T-27 es su primer consumidor externo; (4) un grupo que no cabe en `Configuracion.srt_lineas_max_por_subtitulo` líneas de `srt_caracteres_por_linea_max` caracteres se reparte en varios subtítulos consecutivos — "partición limpia", nunca a mitad de palabra ni con texto descartado (invariante (a), §0.2) —, con el tiempo repartido según el peso en palabras de cada reparto y la última página siempre cerrando exactamente en la duración total del grupo; (5) formato `.srt` estándar en UTF-8 (BOM opcional, `srt_con_bom`), validado por `validar_srt` con las mismas reglas que un lector estricto tipo ffmpeg (índice secuencial, marca de tiempo bien formada, sin solapes ni tiempos decrecientes), reutilizada tanto por la suite como por la cuarta red. `verificar_salidas.py` gana dos etapas nuevas ("Generación del .srt", "Validez del .srt") que sustituyen el `NO APLICABLE` fijo desde T-00, generando y validando de verdad sobre el mismo guion real que ya usa el reproductor. Suite: 337 pasan + 0 skipped (antes 323 + 1; el talón de T-03/T-27 en `test_logica_pendiente.py` se sustituye por 14 tests reales en `tests/test_srt.py`, incluido el criterio de aceptación literal sobre los tres guiones reales — sin solapes, validador estricto en verde, último subtítulo terminando exactamente en la duración total). Las cuatro verificaciones en verde.
>
> **Sesión anterior (2026-09-02) — T-26 (persistencia local de preferencias) COMPLETADA** por una sesión de nube, décima de FASE B4 y última de esa fase (FASE B4 se cierra con esta tarea). Sin hallazgos ABIERTOS de severidad alta en `auditoriacontinua.md` al empezar (el #9 sigue cerrado en código por P-02, pendiente solo de que el auditor lo reevalúe), así que se procedió directo con la cola normal. Extiende el mecanismo mínimo que T-25 adelantó (`claveAlmacenamiento`/`leerPreferencia`/`guardarPreferencia`, ya con `localStorage` envuelto en `try/catch` y clave derivada del guion) al resto de preferencias del requisito 1 — tamaño de texto, velocidad por escena, última escena vista, indicadores —, sin tocar ningún archivo Python: (1) tamaño de texto e indicadores ocultos se restauran con una clave cada uno, aplicando `--tamano-base` de inmediato incluso en el índice, antes de entrar en ninguna escena; (2) la velocidad se guarda por escena bajo `"velocidad_escena_" + escena.numero` (el NÚMERO, no el índice) y la última escena vista bajo el `inicio_segundos` del bloque activo, no su índice — ambas decisiones exigidas por el requisito 5 literal ("si el troceo cambió, la velocidad por escena se conserva y la posición se reajusta al bloque más cercano"): `calcularReanudacion()` reencuentra el bloque cuyo `inicio_segundos` está más cerca del guardado, sobreviviendo a una regeneración con distinto número de bloques; (3) en vez de relanzar el reproductor en automático al cargar la página — `requestFullscreen()` exige un gesto de usuario real, y un intento automático se habría tragado el rechazo del navegador en silencio, dejando una "restauración" a medias sin pantalla completa —, el índice ofrece un botón explícito `#btn-continuar` ("Continuar: escena N — título") que entra en la escena y el bloque guardados con un clic real; de paso corrige un bug preexistente sin efecto visible hasta ahora (`iniciarMotor` marcaba `marcarBloqueActivo(0)` con un literal en vez de la variable `bloqueActual`); (4) "Restablecer preferencias" (requisito 4) es un botón dentro del panel de ayuda (`?`), no una tecla nueva del mapa — borra todas las claves de ese guion en `localStorage` y repone cada valor en memoria a su por defecto sin recargar la página. Verificado a mano con Playwright headless (Chromium, no es dependencia del proyecto) sobre `fixtures/reales/guion-08-busqueda-investigacion.md`: sin sesión previa no aparece "Continuar"; tras interactuar (avanzar bloques, subir velocidad, aumentar tamaño, activar espejo, ocultar indicadores) `localStorage` recoge las seis claves; recargar aplica ya el tamaño en el propio índice y ofrece "Continuar", que al pulsarlo restaura escena, bloque, velocidad, tamaño, espejo e indicadores; "Restablecer preferencias" vacía `localStorage` para ese guion y repone los valores por defecto, y una recarga posterior ya no ofrece "Continuar"; con `localStorage` bloqueado (getter que lanza, simulando `file://` restringido o navegación privada) el reproductor funciona sin errores. Una segunda pasada regenerando el mismo guion con dos troceos distintos (8 y 17 bloques en la misma escena) confirmó el requisito 5: la velocidad se conserva intacta y la posición se reajusta al bloque más cercano por tiempo, no por índice — sin errores de consola en ningún paso. De paso se corrigió una omisión de sesiones anteriores: `SKILL.md` no documentaba el modo espejo de T-25 pese a la regla dura de §0.2 ("todo default configurable y documentado en SKILL.md"); esta sesión añade las secciones de T-25 y T-26 que faltaban. Suite: 323 pasan + 1 skipped (antes 316 + 1; 7 tests nuevos en `tests/test_reproductor.py`). Las cuatro verificaciones en verde. **FASE B4 (reproductor) queda cerrada. Siguiente tarea: T-27 (exportador `.srt` borrador), primera de FASE B5.**
>
> **Sesión anterior (2026-09-02) — T-25 (modo espejo) COMPLETADA** por una sesión de nube, novena de FASE B4. Sin hallazgos ABIERTOS de severidad alta en `auditoriacontinua.md` al empezar (el #9 ya lo cerró en código P-02, pendiente solo de que el auditor lo reevalúe y lo marque RESUELTO en su propio documento), así que se procedió directo con la cola normal. Volteo horizontal del texto para leer contra el cristal de un teleprompter físico: (1) `assets/reproductor/estilo.css` gana dos reglas (`#vista-reproductor.espejo-texto .escena` y `#vista-reproductor.espejo-completo`) que aplican `transform: scaleX(-1)`; `Configuracion.espejo_incluye_indicadores` (nueva, `False` por defecto) decide cuál de las dos usa `guion.js`, así que por defecto el volteo cubre solo el título y los bloques de la escena, nunca la cabecera/barra de progreso/cuenta atrás/ayuda (requisito 1, "sin afectar a la orientación de los indicadores si así se configura"); (2) activable con la nueva acción `espejo` del mapa de teclas (`m`/`M`, añadida a `Configuracion.mapa_teclas_reproductor`) y con un botón nuevo en la cabecera (`#btn-espejo`, agrupado con "Volver al índice" dentro de un `.reproductor-controles` para no romper el `space-between` de la cabecera con un tercer hijo suelto) — ambos disparan la misma `alternarEspejo()`, sin dos implementaciones paralelas; (3) compatible con autoscroll y resaltado (requisito 2) sin tocar ni una línea de `centrarBloqueActivo`/`marcarBloqueActivo`: `scaleX(-1)` solo invierte el eje horizontal, y el centrado de T-22 únicamente mira `rect.top`/`rect.height`; (4) el ajuste persiste tras recargar (requisito 3 y última frase del criterio de aceptación) con tres funciones nuevas y genéricas en `guion.js` (`claveAlmacenamiento`/`leerPreferencia`/`guardarPreferencia`, `localStorage` envuelto en `try/catch`, clave ya derivada del guion) — un adelanto mínimo y deliberado de T-26 (que todavía no existe) limitado a este único ajuste, decisión documentada en `DECISIONES_TECNICAS.md` para que T-26 reutilice esas tres funciones en vez de diseñarlas de nuevo. Verificado a mano con Playwright headless (Chromium, no es dependencia del proyecto) sobre `fixtures/reales/guion-08-busqueda-investigacion.md`: botón y tecla `M` activan/desactivan por igual (mismo `aria-pressed`, mismo `transform` `matrix(-1, 0, 0, 1, 0, 0)` sobre `.escena`); con `espejo_incluye_indicadores=True` el `transform` aparece sobre `#vista-reproductor` entero; tras recargar con el modo activo, el botón sigue en "activado" y el texto sigue volteado sin pulsar nada; con un viewport reducido a 800×300 (para forzar scroll real), la posición vertical del bloque activo tras cada avance es la misma con y sin espejo — sin errores de consola en ningún paso. Suite: 316 pasan + 1 skipped (antes 310 + 1; 6 tests nuevos: 5 en `tests/test_reproductor.py`, 1 en `tests/test_esqueleto.py`). Las cuatro verificaciones en verde.
>
> **Sesión anterior (2026-09-02) — Decisiones del dueño sobre T-24 y T-29 (sin sesión de código), cruzadas con la sesión de nube de T-24.** El dueño **no dispone de clicker Bluetooth** ni del **paquete de `480-branded-pptx`**, y decide: (1) **T-24 se parte** — el mapa de teclado, el antirrebote y la ayuda `?` son implementables y testeables sin hardware (requisitos 1, 3, 4 y la mitad software del 2), y la **calibración del mando físico sale a T-24b, BLOQUEADA hasta nuevo aviso**; se evita así bloquear en cascada T-25 y T-26. **La sesión de nube de T-24 corrió en paralelo sin conocer esta decisión y entregó exactamente ese alcance**, dejando el mando en el bloqueo #5, así que la decisión no revierte nada de lo ya hecho: solo formaliza que **el programador no debe intentar T-24b**. (2) **T-29 se implementa entera tal como está especificada**, apoyándose en su propio requisito 4: con la skill de marca ausente se generan `tarjetas.json` y el brief, la salida `.pptx` queda latente y no se falla; T-30 no se bloquea. Ver §6.9, §6.10, §3 y §7. **Siguiente tarea: T-25 (modo espejo), octava de FASE B4.**
>
> **Sesión de código (2026-09-02) — T-24 (atajos de teclado y clicker Bluetooth) COMPLETADA** por una sesión de nube, séptima de FASE B4. Sin hallazgos ABIERTOS de severidad alta en `auditoriacontinua.md` al empezar, así que se procedió directo con la cola normal. Cierra el mapa de teclas del reproductor que T-19 a T-23 habían ido reservando sobre la marcha, con tres claves nuevas en `Configuracion`/el JSON incrustado (`antirrebote_clicker_ms` —constante ya reservada desde T-20 sin cablear—, `espacio_avanza_bloque`, `mapa_teclas_reproductor`, sin números mágicos): (1) el `switch` de `manejarTeclaReproductor` deja de comparar literales de tecla y pasa a resolver `evento.key` contra una tabla `teclaAAccion` construida desde `datos.mapa_teclas` (una tupla de pares en `Configuracion`, no un `dict`, para conservar la inmutabilidad del dataclass congelado) — remapear una tecla es ahora cambiar `config.py`, no `guion.js`, cumpliendo el requisito 3 ("mapa configurable en la generación"); (2) `Espacio` sigue pausando/reanudando por defecto pero puede avanzar el bloque en su lugar con `espacio_avanza_bloque=True`, para clickers cuyo botón principal envía `Espacio` con sentido de "avanzar" (requisito 1); (3) antirrebote por acción (no global, para no descartar por error dos teclas distintas pulsadas rápido) que descarta una repetición de la misma acción antes de 120 ms por defecto (`0` lo desactiva), aplicado tras cancelar siempre la acción nativa del navegador para evitar el scroll de página incluso en la pulsación descartada (requisito 2); (4) `Esc` (sale de pantalla completa, ya existía desde T-19, ahora documentado en el mapa) y `?` (ayuda en pantalla que lista el mapa vigente, construida leyendo el mismo `datos.mapa_teclas` que usa el manejador de teclado, nunca una copia aparte) completan el mapa. Ningún atajo usa modificadores (`Ctrl`/`Alt`/`Mayús`), que un clicker no puede enviar (requisito 4). Verificado a mano con Playwright headless (Chromium, no es dependencia del proyecto) sobre `fixtures/salida/reproductor.html` (guion real de 7 escenas): la escena se recorrió entera con solo `Espacio`, `PageUp` y `PageDown` — las tres teclas del criterio de aceptación —, dos `PageUp` pegados sin esperar solo retrocedieron un bloque (antirrebote confirmado), `?` abrió un panel legible con las trece acciones del mapa por defecto y una segunda pulsación lo cerró, `Escape` salió de pantalla completa sin ocultar el reproductor, y en un reproductor generado aparte con `espacio_avanza_bloque=True` la tecla `Espacio` avanzó el bloque en vez de pausar — sin errores de consola en ningún paso. Suite: 310 pasan + 1 skipped (antes 300 + 1; 10 tests nuevos: 6 en `tests/test_reproductor.py`, 4 en `tests/test_esqueleto.py`). Las cuatro verificaciones en verde. **Bloqueo #5 de §3 sigue ABIERTO** (queda pendiente que el dueño pruebe el mando físico contra este mapa ya implementado; una sesión de nube no puede verificar hardware Bluetooth real). **Siguiente tarea: T-25 (modo espejo), octava de FASE B4.**
>
> **Sesión anterior (2026-09-02) — T-23 (ayudas de grabación) COMPLETADA** por una sesión de nube, sexta de FASE B4. Sin hallazgos ABIERTOS de severidad alta en `auditoriacontinua.md` al empezar (el #9 ya lo cerró P-02 en la sesión anterior), así que se procedió directo con la cola normal. Cuatro añadidos a `assets/reproductor/guion.js`, todos sobre el `reproducirEscena`/`iniciarMotor`/`detenerMotor` ya existentes de T-19/T-20, con dos claves nuevas en `Configuracion`/el JSON incrustado (`cuenta_atras_segundos`, `cuenta_atras_activada`, sin números mágicos): (1) cuenta atrás 3-2-1 antes de arrancar, desactivable, implementada como una envoltura de `iniciarMotor` (`iniciarCuentaAtras(callback)`) en vez de una fase dentro de él; (2) cronómetro de la toma (tiempo real transcurrido / duración estimada de la escena), recalculado siempre desde marcas de reloj absolutas (`Date.now()`) para que la deriva del criterio de aceptación (<1% en 3 minutos) sea estructural, no medida; se congela y reanuda en pausa igual que el reloj de bloque de T-20; (3) barra de progreso de la escena calculada por recuento de bloques (`(bloqueActual+1)/total`), deliberadamente no por tiempo, para que llegue al 100% exactamente con el último bloque tal como exige el criterio de aceptación, sin depender de que el ritmo real coincida con el estimado; (4) todos los indicadores (cabecera + barra de progreso) ocultables con la tecla `H`/`h` mediante un único interruptor de clase CSS (`indicadores-ocultos` en `#vista-reproductor`) que sobrevive a un cambio de escena. Verificado a mano con Playwright headless (Chromium, no es dependencia del proyecto) sobre `fixtures/salida/reproductor.html`: cuenta atrás visible en "3" al pulsar play, el motor no arranca hasta que termina, desactivada arranca al instante; cronómetro avanza con el reloj real y se congela exactamente en pausa; barra de progreso pasa de 25% a 100% con el último bloque de una escena de 4; `H` oculta y restaura cabecera y barra — sin errores de consola en ningún paso. Suite: 300 pasan + 1 skipped (antes 294 + 1; 7 tests nuevos: 6 en `tests/test_reproductor.py`, 1 en `tests/test_esqueleto.py`). Las cuatro verificaciones en verde.
>
> **Sesión previa (2026-09-02) — P-02 (hallazgo #9, severidad alta) COMPLETADA** por una sesión de nube, atendida como urgente antes de T-23 según manda §0.1/§0.3 al encontrar un hallazgo ABIERTO de severidad alta en `auditoriacontinua.md`. El hallazgo: si en la MISMA revalidación coinciden una edición manual del dueño sobre un bloque y la aceptación de una partición de respiración sobre ese mismo bloque, la identidad que localizaba la edición (`indice_original`, `None`) no sobrevivía a la materialización de la partición (`indice_original`, `'a'`/`'b'`) y la edición se perdía en silencio — invariante (c) roto en ese cruce concreto. Corregido en `scripts/revalidacion.py`: `_materializar_marcados` acepta ahora el conjunto de índices de bloque con edición manual previa a cualquier partición y NO aplica la partición sobre ellos esa pasada (la decisión de aceptarla se conserva en `estado.reescrituras`, solo se pospone su materialización); `_incidencias_conflicto_edicion_particion` deja constancia explícita en el informe, nunca en silencio. Un test nuevo (`test_edicion_manual_y_particion_aceptada_misma_pasada_no_pierde_edicion`) reproduce exactamente el escenario del hallazgo: edición manual + partición aceptada en la misma pasada → la edición sobrevive íntegra, la partición no se materializa, aparece una incidencia. Verificando la corrección a mano se encontró un **límite conocido, registrado pero no cerrado por esta P-XX** (ver `DECISIONES_TECNICAS.md`): en una revalidación POSTERIOR sin nueva edición del dueño, el emparejamiento ancla→identidad puede atribuir el texto editado completo a la mitad `'a'` de la partición en vez de reconocerlo como "todavía sin partición", dejando un bloque `'b'` con un fragmento del texto de ORIGEN sin editar — no es pérdida de texto (el editado sigue íntegro y visible) pero sí contenido incorrecto; se deja para que la próxima auditoría lo registre como hallazgo propio en vez de ampliar el alcance de esta corrección urgente. Suite: 294 pasan + 1 skipped (antes 293 + 1; 1 test nuevo). Las cuatro verificaciones en verde.
>
---

> ## ⚑ PARA EL DUEÑO — empieza por aquí
> Lo único que el proyecto necesita de ti está en dos sitios de este documento:
> - **§3 Bloqueos** = tu lista de tareas. Ahora son **tres** (instalar el paquete de `480-branded-pptx`, probar el clicker Bluetooth real contra el mapa de teclas ya implementado en T-24, y ejecutar tú mismo la instalación de la skill de T-32), y **ninguna de las tres frena el desarrollo**: las dos primeras se entregan igualmente dejando *latente* solo lo que exige el hardware o la skill ausente; la tercera es, directamente, el único paso que le falta a T-32 y que una sesión de nube no puede dar por ti (nota de entorno del protocolo). **No tienes nada urgente que hacer**, salvo que quieras dejar la skill instalada de verdad: ejecuta `python scripts/instalar_skill.py` y luego el health check que te indica, y avísanos del resultado.
> - **§6 Preguntas abiertas** = tus decisiones. **Ninguna pendiente**: las diez están resueltas.
>
> Para control (no exige acción): `DECISIONES_TECNICAS.md` (qué decidió el agente y por qué — sustituye a leer código), `auditoriacontinua.md` (hallazgos abiertos), y aquí §7 (desviaciones) y §5 (P-XX; veta escribiendo `REVERTIR`).

---

## 1. ESTADO GLOBAL DE TAREAS  *(fuente autoritativa de estado y orden de "siguiente tarea")*

| ID | Tarea | Estado | Última sesión | Notas |
|----|-------|--------|---------------|-------|
| T-00 | Verificación inicial | **COMPLETADA** | 2026-08-31 | Esqueleto, 4 redes en verde. El repo ya existía: `git init` no fue necesario |
| T-01 | Linting y formato | **COMPLETADA** | 2026-09-01 | `ruff`/`mypy` estrictos (ya en verde desde T-00) + hook de pre-commit versionado (`scripts/instalar_hooks.py`) que bloquea el commit en rojo |
| T-02 | Logger centralizado | **COMPLETADA** | 2026-09-01 | `scripts/logger.py` (stdlib `logging`), separado de `presentacion.py`; log en la carpeta de salida del guion, `--verbose` cableado en `verificar_salidas.py` |
| T-03 | Suite de tests mínima | **COMPLETADA** | 2026-09-01 | Infraestructura + tests reales de convención contra `fixtures/reales/`; lógica aún no implementada (T-08 a T-13, T-27) cubierta con 8 `skip` con contrato explícito en `tests/test_logica_pendiente.py` |
| T-04 | CI (local + workflow inactivo) | **COMPLETADA** | 2026-09-01 | `scripts/ci.py` (único punto con las cuatro verificaciones); hook delega en él; `.github/workflows/ci.yml` con `workflow_dispatch` únicamente, inactivo |
| T-05 | Monitorización de errores (local) | **COMPLETADA** | 2026-09-01 | `scripts/monitorizacion.py`: `ejecutar_con_diagnostico` (captura + volcado + mensaje accionable) y `ResumenEjecucion` (recuento final). Infraestructura lista; T-07+ la usan desde su punto de entrada |
| T-06 | Robustez de entrada | **COMPLETADA** | 2026-09-01 | `scripts/entrada.py`: valida ruta/tamaño/codificación/estructura mínima, deriva la carpeta de salida de forma segura y acota el tiempo de proceso. `EntradaError` única para todos los fallos |
| T-07 | Estado del proyecto de guión (`estado.json`) | **COMPLETADA** | 2026-09-01 | `scripts/estado.py` + `scripts/migraciones/001_estado_inicial.py`. Escritura atómica, hash de guión, aviso de recalculo |
| T-08 | Parser Markdown y separador de escenas | **COMPLETADA** | 2026-09-01 | `scripts/parser.py`. 7/8/8 escenas exactas en los tres guiones reales, cero preguntas; conflicto de senales y ambiguedad de nivel resueltos con `DeteccionEscenasAmbiguaError` |
| T-09 | Clasificador locución / no locución | **COMPLETADA** | 2026-09-01 | `scripts/clasificador.py`. Ruta rápida por rótulo + cita de bloque; texto suelto en `**LOCUCIÓN**` → `revisar`; inferencia de respaldo ≥95% de precisión sin rótulos; cobertura total verificada por reconstrucción |
| T-10 | Convención de marcado propuesta | **COMPLETADA** | 2026-09-01 | `scripts/convencion.py`. Documenta la convención contractual (`generar_convencion_guiones`/`guardar_convencion_guiones`), señala desviaciones sin bloquear (`detectar_desviaciones`) y deja el mecanismo de consistencia/propuesta (`medir_consistencia_senales`/`proponer_convenciones`) para señales de inferencia futuras |
| T-11 | Troceo en bloques de respiración | **COMPLETADA** | 2026-09-01 | `scripts/troceo.py`. Corta por prioridad (fuerte→débil→nexos→sintagma), protege cifras/fechas/siglas, funde tramos cortos repartiendo si supera el máximo. 100% en rango sobre los tres guiones reales |
| T-12 | Motor de tiempos (ppm deducido del guión) | **COMPLETADA** | 2026-09-01 | `scripts/tiempos.py`. `calcular_tiempos` unica fuente de tiempos; ppm deducido 140-167 en los tres guiones reales (dentro de banda), respaldo 120 ppm documentado, contraste por escena y total |
| T-13 | Normalización a forma dicha | **COMPLETADA** | 2026-09-01 | `scripts/normalizacion.py`. Cardinales/ordinales/porcentajes/monedas/unidades/rangos/fracciones/símbolos/siglas/conjunciones, con diccionario del dueño (`diccionario-locucion.json`) por delante de toda regla automática |
| T-14 | Detector de problemas de locución | **COMPLETADA** | 2026-09-01 | `scripts/deteccion.py`. Cinco familias sobre `BloqueRespiracion` (T-11): frase sin punto de respiración, cacofonías/rima, trabalenguas, anglicismos, estructuras difíciles. Solo la primera admite partición (afecta al troceo); el resto solo avisa |
| T-15 | Reescrituras marcadas y reversibles | **COMPLETADA** | 2026-09-01 | `scripts/reescrituras.py`. Une T-13/T-14 en `Reescritura` (id estable), formato marcado con decisión de una palabra, persistencia append-only en `estado.reescrituras`, aplicación sobre texto y materialización de particiones aceptadas, deshacer global |
| T-16 | `guion-escenas.md` de una sola pasada | **COMPLETADA** | 2026-09-01 | `scripts/documento_revision.py`: compone parseo+tiempos+detección+reescrituras (T-08 a T-15) en un `.md` de revisión con bloques anclados, indicaciones al pie y marca de estado `PENDIENTE`/`VALIDADO` |
| T-17 | Revalidación (respeta ediciones) | **COMPLETADA** | 2026-09-01 | `scripts/revalidacion.py`: relee `guion-escenas.md`, funde con `estado.reescrituras` y materializa/superpone edición manual antes de recalcular tiempos con `tiempos.calcular_tiempos_desde_marcados` (T-12, extraída) |
| T-18 | Reproductor: esqueleto autocontenido | **COMPLETADA** | 2026-09-01 | `scripts/reproductor.py` + `assets/reproductor/`. Datos como JSON en `<script>`, volcados con `textContent`; validador de auto-contención activado de verdad en `verificar_salidas.py --fixture` |
| T-19 | Índice de escenas y pantalla completa | **COMPLETADA** | 2026-09-02 | `assets/reproductor/guion.js` + `estilo.css` reescritos: vista de índice (título, duración, estado pendiente/grabada/revisada) y vista de reproductor, alternadas sin recargar. Fila de escena = único `<button>` navegable con flechas/Tab/Enter/clic |
| T-20 | Motor de avance híbrido | **COMPLETADA** | 2026-09-02 | `assets/reproductor/guion.js`: cadena de `setTimeout` por bloque (duración = `fin_segundos - inicio_segundos` de T-12, escalada por velocidad); pausa/reanudación exacta, avance manual sin salir del automático, velocidad recordada por escena. `Configuracion` gana `paso_velocidad`/`velocidad_minima`/`velocidad_maxima`, viajan al JSON incrustado |
| T-21 | Resaltado, tipografía y tema | **COMPLETADA** | 2026-09-02 | `assets/reproductor/guion.js` + `estilo.css`: atenuación del contexto por distancia (gradiente configurable, opacidad calculada bloque a bloque), contraste AAA del bloque activo verificado por test, tamaño de texto en vivo (`[`/`]`, global, no por escena), margen seguro y cursor oculto en pantalla completa tras inactividad |
| T-22 | Autoscroll con bloque centrado | **COMPLETADA** | 2026-09-02 | `assets/reproductor/guion.js`: `centrarBloqueActivo` mueve `window.scrollY` con interpolacion propia (`requestAnimationFrame`, cancelable) para mantener el bloque activo centrado; corrige de paso un bug real de T-19 (foco diferido que deshacia el centrado) con `focus({preventScroll:true})` |
| T-23 | Ayudas de grabación | **COMPLETADA** | 2026-09-02 | `assets/reproductor/guion.js`: cuenta atrás 3-2-1 desactivable, cronómetro de la toma (deriva estructuralmente nula), barra de progreso por bloques (100% exacto en el último), indicadores ocultables con `H` |
| T-24 | Atajos y clicker Bluetooth | **COMPLETADA** | 2026-09-02 | Mapa completo configurable (`mapa_teclas_reproductor`), antirrebote y ayuda `?`. La verificación del clicker físico **no forma parte de esta tarea**: sale a T-24b por decisión del dueño (§6.9) y sigue en el bloqueo #5 de §3 |
| T-24b | Calibración del clicker Bluetooth real | **BLOQUEADA** — el dueño no dispone de clicker (2026-09-02) | — | Spec: T-24 requisito 2, mitad de verificación física. **El programador no la toca.** Cuando el dueño avise, se resuelve cambiando `Configuracion.mapa_teclas_reproductor`, sin tocar `guion.js`. Ver bloqueo §3.5 |
| T-25 | Modo espejo | **COMPLETADA** | 2026-09-02 | Volteo horizontal (`transform: scaleX(-1)`) de `.escena`, activable con `M`/`m` y con un boton en la cabecera; `Configuracion.espejo_incluye_indicadores` decide si tambien voltea los indicadores. Persistencia minima adelantada de T-26 solo para este ajuste (`claveAlmacenamiento`/`leerPreferencia`/`guardarPreferencia`), que T-26 reutilizara para el resto de preferencias |
| T-26 | Persistencia local de preferencias | **COMPLETADA** | 2026-09-02 | `localStorage` (clave por guion, `try/catch`) para tamaño de texto, velocidad por escena (clave por numero de escena), última escena vista (por `inicio_segundos`, no por índice) e indicadores. Botón "Continuar" en el índice (no auto-lanzamiento: `requestFullscreen` exige gesto de usuario) y "Restablecer preferencias" en la ayuda |
| T-27 | Exportador `.srt` borrador | **COMPLETADA** | 2026-09-02 | `scripts/srt.py`: un subtítulo por bloque de respiración (agrupable si es muy corto, sin cruzar fin de escena), texto locutado final (reescrituras aceptadas incluidas), partición limpia cuando no cabe en el límite de líneas/caracteres, validado con las mismas reglas que ffmpeg |
| T-28 | Exportador `.pdf` con marca 480 | **COMPLETADA** | 2026-09-02 | `scripts/pdf.py`: HTML de impresion (una escena por pagina + portada) con la identidad 480, logotipo incrustado con ratio medido del PNG, conversion con Chrome/Edge headless si esta disponible (nunca falla si no lo esta), modo `--para-terceros` via `incluir_notas_internas` |
| T-29 | Adaptador `.pptx` (`480-branded-pptx`) | **COMPLETADA** | 2026-09-02 | `scripts/pptx.py`: `tarjetas.json` + `brief-pptx.md`; la delegación la hace Claude, no el código. Con la skill de marca ausente (esta máquina) ambos se generan igual y la salida `.pptx` queda latente, tal como preveía su requisito 4 |
| T-30 | Selector de salidas por validación | **COMPLETADA** | 2026-09-02 | `scripts/salidas.py`: pregunta de opción múltiple como datos (`construir_pregunta_salidas`, la formula Claude, no un `input()`), sugerencia desde `estado.salidas_generadas` (sin migración), generación independiente por salida con `try`/`except`, `ResumenSalidas` con generadas/omitidas/latentes |
| T-31 | `SKILL.md` y configuración completa | **COMPLETADA** | 2026-09-02 | Tabla completa de los 81 campos de `Configuracion` en `SKILL.md`, verificada por `tests/test_skill_md.py` (compara claves en las dos direcciones); precedencia documentada; tres referencias nuevas en `references/` (convención de guion, formato de `guion-escenas.md`, mapa de teclas) |
| T-32 | Instalación de la skill y guión de ejemplo | **COMPLETADA** | 2026-09-03 | Desbloqueada: el dueño ejecutó `python scripts/instalar_skill.py` en su máquina (`~/.claude/skills/teleprompter/`) y el health check desde la copia instalada dio **11/11 etapas OK**. Bloqueo #6 de §3 RESUELTO. **Ojo:** la copia instalada quedó hecha ANTES de traer P-03/R-01 del remoto, así que hay que reinstalar para que incorpore ambos |
| T-33 | Encaje con la cadena de montaje | **COMPLETADA** | 2026-09-02 | `references/contrato-montaje.md`: contrato `.srt` + `tarjetas.json`, estructura de carpetas y formula para derivar el rango de tiempo de cada escena; numeracion de escena duplicada/no creciente ahora señalada como desviacion (`convencion.py`); `tests/test_integracion_montaje.py` valida ambas salidas juntas sobre los tres guiones reales |
| R-01 | Persistencia verificada + plan B | **COMPLETADA** | 2026-09-03 | Comprobación real (`try`+relectura al cargar, mas verificacion con Playwright/Chromium real: mismo perfil persiste, perfil nuevo no); aviso honesto si falla; exportar/importar preferencias como `.json` (lee de memoria, no de `localStorage`). Oleada v2 · `origen: auditoría #5` |
| R-02 | Registro de tomas por escena | **COMPLETADA** | 2026-09-03 | Índice = parte de rodaje real: tomas/duración real/toma buena por escena, nota rápida sin salir de grabación, exportación a `.json` fusionada en `estado.json` (migración 002). Oleada v2 · parte de rodaje |
| R-03 | Marcar tropiezos durante la toma | PENDIENTE | — | Oleada v2 · alimenta `FEEDBACK.md` |
| R-04 | Recalibrar el ritmo con tiempos reales | PENDIENTE | — | Oleada v2 · cierra el bucle de T-12 |
| R-05 | `.srt` alineado con la toma buena | PENDIENTE | — | Oleada v3 · continuidad con el montaje |
| R-06 | Coherencia de nombres y `assets/` | PENDIENTE | — | Fase F-D · `origen: auditoría #6` |
| R-07 | Capítulos de YouTube con marcas de tiempo reales | PENDIENTE | — | Oleada v3 · une T-08 (sección auxiliar `Capítulos`) con R-02 (tomas) |
| R-08 | Deuda técnica menor (colores de estado, `PROYECTO.md`, versión de Python) | PENDIENTE | — | Fase F-D · `origen: auditoría #10, #11, #12` |
| R-09 | Endurecer el validador de auto-contención | PENDIENTE | — | Fase F-D · `origen: auditoría #13` |

**Estados:** PENDIENTE · EN CURSO · COMPLETADA · DESPLEGADA EN PRODUCCIÓN · BLOQUEADA — <motivo> · DESCARTADA — <motivo>

*(La spec de cada tarea: T-XX en el cuerpo de `HOJA_DE_RUTA.md`; R-XX en `ROADMAP_PRODUCTO.md`. Este §1 NO repite la spec, solo el estado.)*

---

## 3. BLOQUEOS — ACCIONES PENDIENTES DEL DUEÑO

> El código se entrega igualmente; estas acciones activan funcionalidad latente.

| # | Acción | Tarea | Instrucciones exactas | Estado |
|---|--------|-------|-----------------------|--------|
| 1 | Instalar el plugin `skill-creator` (andamio de la skill) | T-00 | — | **RESUELTO 2026-08-31** — instalado (`skill-creator@claude-plugins-official`, ámbito usuario). |
| 2 | Aportar el **paquete** de `480-branded-pptx` | T-29 | El `SKILL.md` y el `brand-guide.md` ya están transcritos en `references/marca-480.md` (2026-08-31): bastan para el contrato, el brief y el estilo del PDF. Falta la skill instalada en `~/.claude/skills/480-branded-pptx/` con sus assets, y la skill `pptx` de la que depende. Hasta entonces la salida `.pptx` queda latente. **2026-09-02: el dueño confirma que aún no lo tiene y decide NO bloquear T-29** (§6.10) — se implementa entera por su requisito 4 y la salida queda latente, como ya preveía su criterio de aceptación. Este bloqueo no frena ninguna tarea. | ABIERTO — sin urgencia |
| 3 | Aportar 2–3 guiones de producción reales (`.md`) | T-09, T-10 | — | **RESUELTO 2026-08-31** — 3 guiones en `fixtures/reales/` (`guion-08-busqueda-investigacion`, `guion-09-proyectos`, `guion-artefactos-lienzo`). T-08 a T-12 recalibradas contra ellos en la hoja de ruta v1.1. |
| 4 | Conseguir los archivos de logotipo `480_*.png` | T-28 | — | **RESUELTO 2026-08-31** — las cuatro variantes en `assets/`, PNG RGBA con transparencia. **Ojo:** miden 1993×805 (ratio 2,4758), no el 1.7766 que dice la guía de marca; el ratio se mide del archivo. Ver `references/marca-480.md`. |
| 5 | Probar los atajos con el clicker Bluetooth real | T-24b | **2026-09-02: el dueño no dispone de clicker; queda en espera indefinida** (§6.9), y **T-24b está BLOQUEADA — el programador no debe intentarla**. El mapa completo ya está implementado (T-24, 2026-09-02): `Espacio`, `PageUp`/`PageDown`, flechas, `+`/`-`, `[`/`]`, `R`, `H`, `Esc` y `?`. Cuando el dueño consiga un mando: abrir el reproductor generado, pulsar cada botón y decir qué tecla envía cada uno — si no coincide con el mapa vigente (visible con `?` dentro del reproductor), el agente lo ajusta en `Configuracion.mapa_teclas_reproductor`, sin tocar `guion.js`. | ABIERTO — en espera indefinida |
| 6 | Instalar de verdad la skill (T-32) en tu propio Claude Code | T-32 | **2026-09-02: una sesión de nube no alcanza tu `~/.claude/skills/`** (nota de entorno, protocolo v1.3), así que ni la instalación ni el health check posterior pueden ejecutarse ni confirmarse desde aquí — todo lo demás de T-32 (`scripts/instalar_skill.py`, `fixtures/guion-ejemplo.md` y su versión esperada, la documentación de arquitectura en `DEVELOPERS.md`) ya está entregado y probado (contra una copia sincronizada de prueba, nunca contra tu ruta real). Ejecuta `python scripts/instalar_skill.py` (o pásale `--destino` si quieres otra ruta) y luego `python ~/.claude/skills/teleprompter/scripts/verificar_salidas.py --fixture`: si todas las etapas salen `OK`, T-32 puede marcarse COMPLETADA de verdad. Si algo falla, copia aquí la salida completa. | **RESUELTO 2026-09-03** — el dueño instaló la skill en `C:\Users\JanoSolerDíaz\.claude\skills\teleprompter` y el health check desde esa copia salió con las **11 etapas en OK**. Efecto colateral valioso: en su máquina **el `.pdf` real sí se genera** (tiene Chrome/Edge), no queda latente como en las sesiones de nube — es la primera vez que una verificación completa produce el PDF de verdad. La única salida que sigue latente es el `.pptx`, por el bloqueo #2. |

---

## 4. INCIDENTES DE DEPLOY

> Cada vez que una instalación de la skill rompa el health check: qué pasó, qué commit lo causó, cómo se revirtió, qué se aprendió.

| Fecha | Commit causante | Síntoma | Resolución | Lección |
|-------|-----------------|---------|------------|---------|
| —     | Sin incidentes  | —       | —          | —       |

---

## 5. TAREAS AUTOPROPUESTAS (P-XX)

> Registrar aquí cada P-XX ANTES de implementarla (§0.3). El dueño veta con DESCARTAR o REVERTIR en la última columna.

| ID | Descripción | Motivo / valor esperado (incl. `origen: auditoría #N` si aplica) | Estado | Veto del dueño |
|----|-------------|-------------------------------------------------------------------|--------|----------------|
| P-01 | Acotar `.gitignore`: dejar de ignorar `assets/` y `fixtures/` completos | `origen: auditoría #2` (severidad alta). Con las reglas anteriores quedaban fuera del repo los logotipos de marca, los tres guiones de calibración y —en cuanto existan— las plantillas del reproductor y el fixture del health check, con lo que la CI de T-04 no podría reproducir la verificación. Sustituidas por reglas finas que siguen excluyendo lo generado. | **COMPLETADA** 2026-08-31 |  |
| P-02 | Corregir en `revalidacion.py` la pérdida silenciosa de una edición manual cuando coincide, en la misma revalidación, con la aceptación de una partición de respiración sobre el mismo bloque | `origen: auditoría #9` (severidad alta). Rompía el invariante (c) («la edición manual manda») en ese cruce concreto, sin aviso ni test que lo detectara — el dueño podía perder una corrección de texto sin saberlo. Atendida como urgente antes de T-23, según §0.1/§0.3. | **COMPLETADA** 2026-09-02 |  |
| P-03 | Corregir en `revalidacion.py` la duplicación de contenido en una revalidación posterior a un conflicto edición/partición ya pospuesto (P-02), cuando el dueño no vuelve a tocar el documento | `origen: auditoría #14` (severidad alta). El límite que P-02 había dejado documentado como "un fragmento de texto sin editar" resultó ser, verificado con reproducción de código, una duplicación real de contenido en `guion-escenas.md` sin ningún aviso — rompía el invariante (c) por desajuste de esquema de identidad entre pasadas. Atendida como urgente antes de R-01, según §0.1/§0.3. | **COMPLETADA** 2026-09-03 |  |
| P-04 | Endurecer el cierre de `#14`: lectura tolerante de la disposición persistida, persistir solo las particiones realmente pospuestas, incidencia cuando las anclas del documento no son las previstas, y corregir el mensaje del conflicto edición/partición | `origen: auditoría #14` (ya cerrado por P-03, que es correcto). Tres grietas alrededor: (1) `estado.validacion` no se valida al cargar, así que basura en `particiones_pospuestas` tiraba la revalidación entera en vez de degradar; (2) se persistía el conjunto sin filtrar de bloques editados, no el de particiones pospuestas, dejando en `estado.json` índices que nunca tuvieron nada que posponer; (3) **un `estado.json` anterior a P-03, justo en mitad de un conflicto, reproduce el #14 tal cual y en silencio** — sin la clave persistida, la reconstrucción vuelve a ser optimista. Además el aviso del conflicto invitaba a «revalidar sin tocar ese bloque» para materializar la partición, que es justo lo que no funciona: lo hace retirar la edición, como demuestra el test que dejó P-03. | **COMPLETADA** 2026-09-03 |  |
| P-05 | `instalar_skill.py` deja de guardar la copia de seguridad DENTRO de `~/.claude/skills/`; pasa a `~/.claude/teleprompter-copias-de-seguridad/` | **Detectado en vivo en la máquina del dueño** al reinstalar la skill en esta misma sesión: Claude Code registra como skill toda subcarpeta de `~/.claude/skills/` que tenga un `SKILL.md`, así que `teleprompter.bak-<marca>` apareció en la lista de skills disponibles **como una segunda `teleprompter`, con nombre y descripción idénticos**, compitiendo con la real en la selección. Cada reinstalación añadía una. No es cosmético: degrada la funcionalidad principal de la skill justo cuando el dueño va a usarla. La copia de la instalación de hoy se movió a mano fuera de `skills/`; el arreglo evita que vuelva a pasar. | **COMPLETADA** 2026-09-03 |  |

---

## 6. PREGUNTAS ABIERTAS PARA EL DUEÑO

> Decisiones que los agentes no pueden tomar. El dueño responde en la última columna.

| # | Pregunta | Tarea | Respuesta |
|---|----------|-------|-----------|
| 1 | ¿Confirmas 120 ppm como ritmo base, o prefieres el ppm implícito que se deduce de las duraciones objetivo de tus propios encabezados (T-12 lo calcula)? | T-12 | **ppm implícito de cada guión** (2026-08-31). Se deduce como valor único por guión, no por escena, para no anular el aviso de desviación. Respaldo 120 ppm si el guión no trae duraciones objetivo o el valor cae fuera de 90–180. → §0.2 y T-12. |
| 2 | ¿Qué nivel de encabezado usas hoy como escena en tus guiones (`#`, `##` o `###`), o prefieres que se detecte cada vez? | T-08 | *Respondida por evidencia (2026-08-31):* nivel `##` con patrón `BLOQUE N — <título> (m:ss – m:ss)`. Confirma solo si algún guión tuyo se sale de este patrón. |
| 3 | Tus guiones ya usan una convención estable (`**LOCUCIÓN**` en cita de bloque, `**EN PANTALLA**`, `**NOTA**`). ¿La fijamos como contractual, de modo que la skill deje de inferir y avise cuando un guión se salga de ella? | T-10 | **Contractual, con aviso** (2026-08-31). Los rótulos mandan; una escena sin rótulo se procesa infiriendo y se señala como desviación, nunca se convierte en error. → §0.2, T-09 y T-10. |
| 4 | Alcance de las reescrituras: ¿solo normalización a forma dicha y respiración, o también estilo (anglicismos, estructuras difíciles)? | T-15 | **Solo forma dicha y respiración** (2026-08-31). Cacofonías, trabalenguas, anglicismos y estructuras difíciles se avisan pero no se reescriben. Ampliar el alcance es decisión del dueño, no una P-XX. → §0.2, T-14 y T-15. |
| 5 | ¿El `.pdf` debe llevar la marca 480 en versión oscura (pantalla) o clara (impresión en papel)? | T-28 | **Clara, fondo blanco** (2026-08-31), alineado con la guía de `480-branded-pptx`. Uso: documento de repaso y, llegado el caso, entregable a terceros → una escena por página, locución como prosa legible y bandera `--para-terceros` que omite notas internas. → T-28 y T-29. |
| 6 | La guía de marca dice **Poppins** («familia oficial») y el `SKILL.md` de `480-branded-pptx` dice **Figtree** («familia obligatoria»). ¿Cuál manda? | T-28, T-29 | **Poppins** (2026-08-31): manda la guía de marca. El PDF sale en Poppins y el brief de T-29 se la pide también a la skill de marca, para que los dos documentos coincidan. Clave `tipografia_marca`. |
| 7 | **La rama de trabajo del protocolo no existe.** El repo está en `develop`, con `master` y remoto `origin` en GitHub; el protocolo decía `main`. | §0.1 · `origen: auditoría #1` | **`develop`** (2026-08-31). Los agentes commitean en `develop`; **`master` es del dueño**, que hace el merge manualmente. Protocolo actualizado (hoja de ruta v1.2, §0.1 y §0.2) y prompts de los tres agentes corregidos. |
| 8 | **Poppins no está instalada en esta máquina** (Figtree sí, Montserrat no). ¿Instalas Poppins, o cambiamos a Figtree? | T-28, T-29 · `origen: auditoría #3` | **Poppins instalada** (2026-08-31). Verificado: 5 archivos — Bold, SemiBold, Medium, Regular y Light —, que cubren toda la escala tipográfica de la guía de marca. La decisión se mantiene y ahora sí es efectiva. |
| 9 | **T-24 depende del clicker Bluetooth, que el dueño no tiene.** ¿Se bloquea la tarea entera (y con ella T-25 y T-26, que dependen de ella en cascada) o se parte? | T-24, T-25, T-26 | **Partirla** (2026-09-02). El clicker se identifica como un teclado corriente, así que el mapa completo, el antirrebote y la ayuda `?` son implementables y testeables sin hardware; lo único que exige el mando físico es saber qué botón manda qué tecla. **T-24 se implementa con alcance reducido** (requisitos 1, 3, 4 y la mitad software del 2) y **la calibración sale a T-24b, BLOQUEADA hasta nuevo aviso del dueño**. FASE B4 continúa: T-25 y T-26 no se bloquean. **Nota del mismo día:** la sesión de nube de T-24 corrió en paralelo sin conocer esta decisión y entregó exactamente ese alcance, así que T-24 quedó COMPLETADA y la decisión no revierte nada — solo formaliza T-24b. → §1, §3.5 y §7. |
| 10 | **El paquete de `480-branded-pptx` sigue sin estar disponible.** ¿Se bloquea T-29 (y con ella T-30, T-31, T-32 y T-33 en cascada)? | T-29, T-30 | **No se bloquea** (2026-09-02). T-29 ya está especificada para funcionar con la skill de marca ausente: su requisito 4 y su criterio de aceptación exigen que se generen `tarjetas.json` y el brief, que la salida se marque latente y que no falle. Se implementa entera; **solo la generación real del `.pptx` queda latente** hasta que el dueño aporte el paquete (bloqueo §3.2). → §1, §3.2 y §7. |

---

## 7. DESVIACIONES RESPECTO A LA HOJA DE RUTA ORIGINAL

> Resumen consolidado para comparar contra `HOJA_DE_RUTA.md` de un vistazo.

| Fecha | Tarea | Desviación | Motivo |
|-------|-------|-----------|--------|
| 2026-08-31 | T-00 | Se trabajó y se commiteó en `develop`, no en `main` como fijaba §0.1 | **Desviación cerrada el mismo día:** el dueño resolvió §6.7 confirmando `develop` como rama de trabajo y `master` como suya para el merge manual. El protocolo se actualizó (hoja de ruta v1.2) y a partir de ahí `develop` deja de ser una desviación: es la norma. |
| 2026-08-31 | T-00 | No se ejecutó `git init` | Ya estaba hecho. El resto de la tarea se cumplió íntegro. |
| 2026-09-02 | T-24 | La tarea se parte en dos: **T-24** (mapa de teclado, antirrebote, ayuda `?`) y **T-24b** (calibración del clicker físico, BLOQUEADA). La hoja de ruta la define como una sola tarea | **Decisión del dueño** (§6.9): no dispone de clicker. Bloquear T-24 entera habría bloqueado en cascada T-25 y T-26 y parado toda FASE B4, cuando la mitad software es implementable y testeable sin hardware. `HOJA_DE_RUTA.md` no se modifica (es inmutable): la spec de T-24b es el requisito 2 de T-24 en su mitad de verificación física. La sesión de nube de T-24, en paralelo y sin conocer la decisión, entregó ese mismo alcance por su cuenta: T-24 queda COMPLETADA y la desviación se reduce a la existencia de T-24b. |
| 2026-09-02 | T-29 | Ninguna desviación de la spec; se deja constancia de que **NO se bloquea** pese a seguir sin el paquete de marca | **Decisión del dueño** (§6.10). Se registra aquí porque la nota «Bloqueo humano» de T-29 en la hoja de ruta podía leerse como motivo para marcarla BLOQUEADA, cuando su propio requisito 4 y su criterio de aceptación exigen justo lo contrario: entregar el código completo con la salida latente. |
| 2026-08-31 | T-00 | No se hizo push al remoto | El protocolo §0.1 contemplaba push porque en un proyecto web equivale a desplegar. Aquí el despliegue es la instalación local de la skill (T-32) y publicar en GitHub es una acción hacia fuera. **Incorporado a la norma en v1.2:** no se hace push sin autorización explícita del dueño. Deja de ser desviación. |
