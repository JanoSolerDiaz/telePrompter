# DECISIONES TÉCNICAS — teleprompter

> Registro **append-only** de decisiones técnicas tomadas de forma autónoma (antiguo §2 de
> SEGUIMIENTO). Es la sección que **sustituye a la revisión de código**: el dueño audita aquí.
>
> Disciplina del programador:
> - **Consulta** este documento (en el área que vas a tocar) ANTES de cualquier decisión técnica
>   no trivial, para no contradecir decisiones previas.
> - **Añade** cada nueva decisión relevante al cerrar la sesión (no edites ni borres filas previas).
> - Si una decisión es **norma permanente** ("usamos X, no reintroducir Y"), promuévela además a
>   §0.2 de `HOJA_DE_RUTA.md`, que se lee en cada sesión.
>
> Rotación: si crece mucho, archivar por trimestre en `roadmap/DECISIONES_TECNICAS/AAAA-Tn.md`
> con un índice al inicio.

| Fecha | Tarea | Decisión | Alternativas consideradas | Por qué |
|-------|-------|----------|---------------------------|---------|
| 2026-08-31 | T-00 | No ejecutar el scaffolder de `skill-creator`; seguir su convención a mano (`SKILL.md` con frontmatter, `references/`, `assets/`, `scripts/`) | Ejecutar `skill-creator` tal cual | Su scaffolder crea el paquete directamente en `~/.claude/skills/<nombre>/`, que en este proyecto es el **destino de instalación** (T-32), no el repositorio de desarrollo. Ejecutarlo ahora crearía la copia instalada antes de que exista el código fuente. Su convención sí se respeta íntegra. |
| 2026-08-31 | T-00 | Herramientas de desarrollo instaladas con `pip install --user` en el intérprete del sistema, no en un entorno virtual | `.venv` en el repositorio | El protocolo §0.1 fija los comandos de verificación como `python -m mypy …`. Con un entorno virtual, esos comandos solo son ciertos si está activado, y el arnés de ejecución no conserva estado entre llamadas. Con `--user` los comandos del protocolo son literalmente ejecutables. Revisable si el proyecto crece. |
| 2026-08-31 | T-00 | La cuarta red (`verificar_salidas.py`) declara cada etapa **NO APLICABLE** nombrando la tarea que la implementará, en vez de pasar en vacío o fallar siempre | Devolver siempre 0 hasta T-32; o dejar la cuarta red desactivada | Responde al hallazgo #4 del auditor. Una red que miente educa a saltársela. Así dice siempre algo verdadero y se activa sola: cuando T-18 genere el HTML, la comprobación de auto-contención deja de ser NO APLICABLE sin tocar el protocolo. |
| 2026-08-31 | T-00 | Regla de lint `T20` activada: `print()` prohibido fuera de `scripts/presentacion.py` | Confiar en la revisión | Convierte la regla §0.2 «logger centralizado, nunca logging de desarrollo en producción» en algo que la máquina comprueba, no algo que hay que recordar. |
| 2026-08-31 | §0.1 / §0.2 | Las rutinas pasan a **agentes de nube** sobre un clon de `github.com/JanoSolerDiaz/telePrompter`; el **push a `origin/develop` pasa a ser obligatorio** al cerrar sesión (hoja de ruta v1.3) | Mantenerlas sobre la máquina del dueño vía el conector de dispositivos remotos, sin push | Decisión del dueño. Un contenedor de nube se destruye al terminar: sin push, el trabajo de la sesión se pierde. La norma anterior («no publicar sin autorización») era coherente con agentes locales e incompatible con agentes de nube. `master` sigue intocable. |
| 2026-08-31 | Rutinas | Las tres rutinas cambian de entorno (`…111117` → `Default` de nube), reciben el repositorio como `source` y las herramientas `Bash/Read/Write/Edit/Glob/Grep` | Añadir solo el repositorio | Sin herramientas de archivos el agente no puede tocar el clon, y el entorno de dispositivo remoto no clona. Añadir el repositorio sin lo demás habría dejado las rutinas peor que antes: con un repositorio que no podrían usar. |
| 2026-08-31 | Rutinas | Preámbulo obligatorio de tres puntos en los tres prompts (rama, clon efímero, `pip install -r requirements-dev.txt`) | Confiar en que el agente lo deduzca | Ninguna de las tres cosas es deducible desde dentro del contenedor, y las tres son causa de fallo silencioso: trabajar en la rama equivocada, terminar sin push, o dar por buena una verificación que no se pudo ejecutar. |
| 2026-08-31 | §0.1 / §0.2 | Rama de trabajo `develop`; `master` intocable para los agentes; push solo con autorización explícita | Crear `main`; trabajar en `master`; push automático | Decisión del dueño al resolver §6.7. Es la única circunstancia en que la hoja de ruta se modifica: el protocolo lo cambia el dueño. Promovido a §0.2 (norma permanente) en lugar de quedarse como desviación recurrente en §7, para que ninguna sesión futura vuelva a derivar el conflicto. |
| 2026-08-31 | T-28 | Se mantiene **Poppins** como tipografía de marca del `.pdf` | Cambiar a Figtree, ya instalada | El dueño instaló Poppins al conocer el hallazgo #3. Verificado: 5 pesos (Bold, SemiBold, Medium, Regular, Light) que cubren la escala completa de la guía. La decisión original deja de ser teórica y no hace falta tocar `tipografia_marca`. |
| 2026-08-31 | P-01 | `.gitignore` acotado a artefactos generados; `assets/` y `fixtures/` pasan a versionarse | Mantener la exclusión y documentar los archivos aparte | `origen: auditoría #2`. Con la exclusión anterior, los logotipos, los guiones de calibración y las futuras plantillas del reproductor quedaban fuera del repositorio: la verificación no sería reproducible en otra máquina ni en la CI de T-04. |
| 2026-09-01 | T-01 | Hook de pre-commit versionado en `scripts/hooks/pre-commit` (plantilla) + `scripts/instalar_hooks.py` (instalador idempotente que resuelve `.git/hooks` incluso en worktrees), en vez de escribir directamente en `.git/hooks/pre-commit` | Instalar el hook a mano en `.git/hooks/`; usar el framework `pre-commit` (paquete de terceros) | `.git/hooks/` no está bajo control de versión: en una sesión de nube sobre un clon efímero, un hook no versionado desaparece al terminar la sesión y ninguna sesión futura lo hereda. Versionando la plantilla y con un instalador de una sola línea, el hook sobrevive a cualquier clon. Se descarta el framework `pre-commit` por ser una dependencia de desarrollo adicional sin necesidad real: las cuatro verificaciones ya son cuatro comandos directos del protocolo. El hook ejecuta las cuatro verificaciones completas (mypy, ruff, pytest, `verificar_salidas.py --fixture`), no solo mypy/ruff, siguiendo el requisito de §0.1 de que ninguna verificación en rojo llegue a commit. Probado en real: bloqueó un commit con un error de sintaxis deliberado y dejó pasar uno limpio. |
| 2026-09-01 | T-02 | `scripts/logger.py` con el modulo `logging` de la biblioteca estandar, separado de `scripts/presentacion.py`: presentación habla con el dueño (español, resultado de la ejecución), logger diagnostica la ejecución (técnico, para depurar). `configurar_logger(carpeta_salida, verbose=...)` escribe siempre `<carpeta_salida>/teleprompter.log` en DEBUG; `--verbose` solo añade un `StreamHandler` a stderr en INFO. Idempotente (limpia manejadores previos del logger nombrado antes de reconfigurar) para no acumular manejadores entre llamadas repetidas en el mismo proceso (tests, revalidaciones). | Un logger por módulo con `logging.getLogger(__name__)`; escribir el diagnóstico a través de `presentacion.py` en vez de un canal aparte | Un logger por módulo dispersaría la configuración (formato, ruta, nivel) y violaría "módulo único de salida" (§0.2). Mezclarlo con `presentacion.py` confundiría audiencia (el dueño) con propósito (depurar un fallo): el dueño no debería ver trazas técnicas, y los diagnósticos no deben aparecer en el `guion-escenas.md` ni en la salida que se lee en caliente. El log vive siempre dentro de la carpeta de salida del guion, nunca en una ruta ajena (regla de aislamiento, §0.2); en `verificar_salidas.py` esa carpeta es `fixtures/salida/`, que pasa a ignorarse en git por ser artefacto generado. |
| 2026-09-01 | T-03 | El criterio de aceptación de T-03 nombra pruebas sobre lógica (parser, clasificador, troceador, motor de tiempos, normalizador, `.srt`) que aún no existe: esas tareas (T-08 a T-13, T-27) van DESPUÉS de T-03 en el orden de §1 y solo dependen de T-01. Se resuelve el hueco tratando cada capacidad pendiente como una etapa "NO APLICABLE" del mismo tipo que introdujo T-00 en `verificar_salidas.py`, pero expresada como test: `tests/test_logica_pendiente.py` reúne 8 tests con `@pytest.mark.skip(reason="T-XX: …")`, uno por capacidad más las dos invariantes de §0.2 ((a) cobertura total y (c) idempotencia), cada uno con un docstring que especifica lo que deberá comprobar. Lo que sí existe hoy (el contrato de convención en `config.py`: `PATRON_ENCABEZADO_ESCENA`, `ROTULO_LOCUCION`, `ROTULOS_NO_LOCUCION`) se cubre con tests reales en `tests/test_convencion_guiones_reales.py` contra los tres guiones de `fixtures/reales/`, más `tests/conftest.py` con las fixtures `guiones_reales`/`texto_guiones_reales` para que T-08 y siguientes no repitan esa carga. | (a) Dejar T-03 "COMPLETADA" sin más porque el código existente ya tiene tests — deja el hueco documental invisible, exactamente el riesgo que el hallazgo #4 del auditor señaló para la cuarta red; (b) Adelantar la implementación de T-08/T-09/etc. dentro de T-03 para poder escribir los tests "de verdad" — viola el orden de §0.1 ("no saltes a una posterior si la anterior no está COMPLETADA") y el principio de no construir por adelantado; (c) Dejar las pruebas pendientes sin registrar en ningún test, solo como texto en este documento — no aparece en la salida de `pytest`, así que una sesión futura puede olvidarlo sin que nada se lo recuerde | La opción elegida es la única que dice siempre la verdad en `pytest -q` (8 `skipped` visibles) y convierte cada hueco en un contrato explícito y verificable: cuando T-08 implemente el parser, quitar el `skip` de `test_parser_reconoce_toda_escena_del_guion_real` sin más ya es una prueba en rojo que el propio código de T-08 debe poner en verde, en vez de una tarea de documentación aparte que se puede olvidar. T-03 queda COMPLETADA hoy para el alcance que es honesto completar antes de T-08; el resto se activa solo, tarea a tarea, igual que ya ocurre con la cuarta red desde T-00. |
| 2026-09-01 | T-04 | `scripts/ci.py` centraliza las cuatro verificaciones del protocolo (antes duplicadas en `scripts/hooks/pre-commit` y en `DEVELOPERS.md`); las ejecuta TODAS en orden, incluso si una falla, imprime un resumen etapa a etapa (reutilizando `presentacion.py`, igual que la cuarta red de `verificar_salidas.py`) y agrega el resultado en un único código de salida (0/1). El hook de pre-commit pasa a ser `python scripts/ci.py` + `exit $?`, sin duplicar los cuatro comandos. | (a) Mantener los cuatro comandos duplicados en el hook y añadir `ci.py` como una tercera copia — exactamente el problema que motiva la tarea (tres sitios que mantener sincronizados); (b) Que `ci.py` aborte en la primera etapa que falla (fail-fast), como ya hacía el hook — más rápido, pero el propio enunciado de T-04 pide "salida resumida" de las cuatro, y un resumen de una sola etapa no informa nada que `mypy`/`ruff`/`pytest` sueltos no den ya; (c) Usar el framework `pre-commit` de terceros para orquestar las etapas — ya descartado en la decisión de T-01 (dependencia de desarrollo sin necesidad real) y sigue aplicando aquí | `ci.py` como único punto de verdad hace que el hook, la guía para desarrolladores y una futura CI remota lean la misma fuente; correr las cuatro etapas siempre hasta el final da, en cada intento, la foto completa de qué está roto y qué no, en vez de descubrirlo una etapa por commit. Se actualizaron los tests que antes comprobaban el contenido literal del hook (`mypy`, `ruff check`, etc. dentro del `.sh`): ahora comprueban que delega en `ci.py` y que su código de salida se propaga end-to-end (`sh` real con un `ci.py` de mentira que sale en 0 o en 1), que es lo que el hook realmente garantiza tras el cambio. |
| 2026-09-01 | T-04 | El workflow de GitHub Actions (`.github/workflows/ci.yml`) se deja preparado pero **inactivo a propósito**: solo dispara con `workflow_dispatch` (manual), sin `on: push` ni `on: pull_request` sobre `origin/develop`, aunque el repositorio ya tiene remoto desde que las rutinas pasaron a la nube (v1.3). | Activarlo automáticamente en `push`/`pull_request` ahora que hay remoto real | El enunciado de T-04 pide explícitamente un workflow "inactivo... para el día que haya remoto"; que el remoto ya exista no convierte esa frase en papel mojado — decidir que la CI remota se dispare sola sobre cada push a `origin/develop` (con sus notificaciones, minutos de Actions y expectativas de que alguien la mire) es una decisión de proceso del dueño, no algo que esta tarea deba tomar por su cuenta. Queda listo para activar cambiando un único bloque `on:`. |
| 2026-09-01 | T-05 | `scripts/monitorizacion.py` con dos piezas: `ejecutar_con_diagnostico(funcion, carpeta_salida)` envuelve el futuro punto de entrada de la CLI, captura cualquier `Exception` no controlada, vuelca tipo+mensaje+traceback (sin variables locales) a `<carpeta_salida>/diagnostico-<timestamp>.log`, registra el mismo evento en el logger de T-02 y muestra al dueño solo un mensaje accionable genérico por `presentacion.py` (nunca la traza cruda); y `ResumenEjecucion`, una dataclass con los cinco recuentos que pide el criterio de aceptación (escenas, bloques, avisos, reescrituras, salidas) y un método `mostrar_resumen()`. Como en T-02/T-04, es infraestructura sin punto de entrada real todavía: T-07 en adelante son quienes deben envolver su `main()` con `ejecutar_con_diagnostico` y rellenar `ResumenEjecucion` al terminar. | (a) Volcar también los valores de las variables locales del marco (`traceback.TracebackException(..., capture_locals=True)`), que da un diagnóstico más rico; (b) Enganchar `sys.excepthook` globalmente en vez de un wrapper explícito de función | (a) se descarta porque viola la regla dura de T-05 ("el diagnóstico no incluye el contenido íntegro del guión"): una variable local con el texto completo del guión en el marco donde salta la excepción quedaría volcada entera. Se deja un test que fija el comportamiento por defecto de `traceback.format_exception` (sin `capture_locals`) como regresión: si alguien lo activa más adelante creyendo que mejora el diagnóstico, el test lo detecta. (b) se descarta porque no hay todavía un único punto de entrada de proceso que enganchar (no existe `main()` real hasta T-07+), y un wrapper de función explícito es más fácil de testear sin lanzar un subproceso real, igual que se hizo con `ci.py` en T-04. |
| 2026-09-01 | T-06 | `scripts/entrada.py` nuevo, delante del futuro parser (T-08): `validar_ruta_guion` (existe/no es carpeta/tamano <= `TAMANO_GUION_MAX_BYTES`), `leer_guion` (decodifica UTF-8 con o sin BOM via `utf-8-sig`, rechaza cualquier otra codificacion, rechaza vacio/solo blancos), `verificar_estructura_minima` (cuenta encabezados de CUALQUIER nivel — no el patron estricto de escena — y rechaza 0 o > `ESCENAS_MAX`), `nombre_guion_seguro`/`carpeta_salida_para` (derivan `<carpeta-del-guion>/<nombre-guion>-tarjetas/` saneando el nombre y verificando por `Path.relative_to` que no cae fuera de la carpeta del guion) y `ejecutar_con_limite_de_tiempo` (tope de tiempo con `ThreadPoolExecutor.result(timeout=...)`, no con `signal.alarm`). Toda la superficie usa una unica excepcion, `EntradaError`, con mensaje ya accionable en espanol. `config.py` gana `TIEMPO_PROCESO_MAX_SEGUNDOS` (60 s por defecto) junto a `TAMANO_GUION_MAX_BYTES`/`ESCENAS_MAX`, que ya existian reservados desde T-00. 20 tests nuevos en `tests/test_entrada.py`, incluida una entrada con ruta con acentos y espacios (Windows). | (a) Usar `signal.alarm`/`SIGALRM` para el tope de tiempo — descartado: no existe en Windows, la maquina del dueno; (b) Contar solo los encabezados que casan con `PATRON_ENCABEZADO_ESCENA` (T-08) para el guardado de "demasiadas escenas" — descartado porque una entrada hostil que evite a proposito ese patron (encabezados `##` sin `BLOQUE N` ni marca de tiempo) lo esquivaria sin esfuerzo, dejando el limite de recursos sin efecto justo donde mas hace falta; (c) Que esta capa decida ella misma que encabezado es escena y cual es seccion auxiliar — descartado por invadir el trabajo de T-08, que ademas puede preguntar al dueno ante ambiguedad (T-08, requisito 6) | El criterio de aceptacion de T-06 pide una bateria de entradas hostiles en verde que "degradan con error accionable, ninguna rompe ni escribe fuera": una unica excepcion con mensaje ya en espanol permite que el futuro `main()` (T-07+) solo tenga que capturar `EntradaError` y mostrarla, sin logica de traduccion. Se opto por contar encabezados de cualquier nivel (no el patron estricto) porque el objetivo es acotar el *coste computacional* de la etapa siguiente, no acertar el numero real de escenas — eso sigue siendo decision de T-08. `ejecutar_con_limite_de_tiempo` no cancela el hilo huerfano (Python no lo permite): es una garantia sobre el tiempo del dueno, documentada como tal en el docstring para que nadie la confunda con una cancelacion real. Efecto colateral registrado aparte: al escribir el primer generico de la skill se detecto que el `python` real de este contenedor es 3.11.15 pese a `requires-python = ">=3.12"` en `pyproject.toml`; se evito la sintaxis PEP 695 (`def f[T](...)`) por eso, no por preferencia de estilo — ver la fila siguiente. |
| 2026-09-01 | T-07 | `scripts/estado.py` nuevo: `EstadoProyecto` (dataclass) fija el contrato de `estado.json` (version de esquema, `InfoGuion` con ruta/hash sha256/tamano, configuracion efectiva, `SeparadorEscena` nivel/patron para T-08, y contenedores vacios `escenas`/`reescrituras`/`validacion`/`salidas_generadas` que T-08 en adelante rellenan). `guardar_estado` escribe de forma atomica: `estado.json.tmp` en la misma carpeta + `Path.replace()` (atomico en POSIX y Windows); si falla antes del reemplazo, limpia el temporal y el `estado.json` anterior queda intacto. `cargar_estado` lee, migra y reconstruye, levantando `EstadoError` (mismo contrato que `EntradaError` de T-06: mensaje ya accionable) ante archivo ausente, JSON corrupto o estructura incompleta tras migrar. `guion_modificado`/`avisar_si_guion_modificado` comparan hash sha256 contra el guardado y avisan por `presentacion.py` que la proxima pasada recalculara desde cero (todavia no hay recalculo incremental). 34 tests nuevos en `tests/test_estado.py` y `tests/test_migraciones.py`, incluido uno que simula un corte a mitad de escritura (`Path.replace` monkeypatcheado para fallar) y comprueba que el `estado.json` anterior sobrevive intacto — cubre literalmente el criterio de aceptacion "proceso interrumpido a mitad y relanzado que reanuda sin perdida". | Usar `os.replace()` en vez de `Path.replace()` para la escritura atomica | `os.replace()` es igual de atomico, pero la regla `PTH105` de ruff (ya seleccionada en `pyproject.toml`, categoria "usar pathlib") lo prohibe en favor de `Path.replace()`, que internamente hace lo mismo. Se opto por seguir la regla ya vigente en vez de anadir una excepcion nueva. |
| 2026-09-01 | T-07 | Mecanismo de migraciones: `scripts/migraciones/NNN_<nombre>.py`, cada archivo con `VERSION_DESTINO: int` y `aplicar(datos: dict) -> dict` a nivel de modulo; `migraciones/__init__.py` descubre los archivos por el prefijo numerico del nombre, los importa con `importlib.import_module` (no con `import` normal) y los aplica en cadena segun `datos.get("version_esquema", 0)`. Regla `N999` de ruff ignorada solo para `scripts/migraciones/*.py` (nuevo `per-file-ignores` en `pyproject.toml`). | (a) Registrar las migraciones a mano en una lista dentro de `__init__.py` en vez de descubrirlas por nombre de archivo; (b) Nombrar los archivos sin prefijo numerico (`estado_inicial.py`) y guardar el orden en otro sitio; (c) Usar un framework de migraciones de terceros | (a) y (b) obligarian a mantener el orden en dos sitios (el nombre del archivo y una lista aparte), exactamente el problema que la tarea T-04 evito para las cuatro verificaciones. El enunciado de T-07 pide explicitamente el patron `NNN_<nombre>.py`, que es el que usa Django para sus migraciones — mismo mecanismo de carga (`importlib.import_module` con un nombre de modulo que empieza por digito, verificado que funciona en este interprete antes de escribir el codigo). (c) descartado por la misma razon que en T-01/T-04: dependencia de desarrollo sin necesidad real, con una migracion que hoy es solo "crear el esquema inicial". |
| 2026-09-01 | T-07 | La migracion `001_estado_inicial.py` trata cualquier `dict` sin `version_esquema` (o con un valor menor que 1) como "version anterior", incluido un `estado.json` vacio o parcial: preserva toda clave ya presente y solo completa con valores por defecto las que falten (`configuracion_efectiva`, `separador_escena`, `escenas`, `reescrituras`, `validacion`, `salidas_generadas`) | Esperar a que exista una version 0 "real" con su propio esquema documentado antes de escribir la migracion 001 | El enunciado de T-07 pide el mecanismo de migraciones y su test de aceptacion ("aplica la migracion 001 sobre un estado.json de version anterior sin perder validaciones ni ediciones") en la MISMA tarea que crea el esquema por primera vez: no hay una version 0 real que preceda a T-07. Tratar "sin `version_esquema`" como la version anterior implicita permite escribir el test pedido literalmente (un dict con validaciones/reescrituras ya presentes que se migra sin perderlas) sin inventar un esquema previo ficticio solo para justificar la migracion. |
| 2026-09-01 | T-06 | Se evita la sintaxis de generics de PEP 695 (`def f[T](...)`, la que sugiere por defecto la regla `UP047` de ruff) en `entrada.py`; se usa `typing.TypeVar` clasico con `# noqa: UP047` | Activar la sintaxis nueva, ya que `pyproject.toml` fija `target-version = "py312"` y `requires-python = ">=3.12"` | El comando exacto del protocolo (`python -m mypy scripts/ tests/`) se ejecuta en este contenedor con el `python` del PATH, que resuelve a 3.11.15 (`python3.12` existe aparte, sin usarse). `ast.parse` de 3.11 no reconoce la sintaxis PEP 695 en absoluto (no es un aviso de mypy, es un error de sintaxis), asi que usar `def f[T]` habria roto la verificacion pre-push tal como esta definida hoy, sin tocar codigo de producto. No se toca `pyproject.toml` ni se investiga mas a fondo por quedar fuera del alcance de T-06; queda anotado aqui para que una sesion futura no repita el tropiezo si vuelve a necesitar sintaxis exclusiva de 3.12+. |
| 2026-09-01 | T-08 | `scripts/parser.py`: clasificacion de cada encabezado del nivel separador en tres pasos -- (1) casa con `PATRON_ENCABEZADO_ESCENA` -> escena; (2) si no, se miran a la vez la lista negra (`configuracion.secciones_auxiliares`, senal terciaria) y el rotulo `**LOCUCIÓN**` en el cuerpo (senal secundaria): ninguna de las dos -> auxiliar (caso por defecto); solo lista negra -> auxiliar; solo locucion -> escena (la senal secundaria basta para promover, no hace falta preguntar); **las dos a la vez -> conflicto real**, se levanta `DeteccionEscenasAmbiguaError` en vez de decidir. Se descarto interpretar "senal secundaria" como un disparador de pregunta por si sola (bastaria con no encontrar el patron y tener locucion para preguntar) | Preguntar siempre que el patron no case, aunque solo aplique una senal (la secundaria o la terciaria, no ambas) | Con "preguntar si cualquier senal no es unanime" los tres guiones reales habrian disparado la pregunta en el subtitulo entrecomillado y en las tres secciones de lista negra (ninguna tiene senal secundaria a favor, pero tampoco "unanimidad" en el sentido estricto), incumpliendo el criterio de aceptacion literal de T-08 ("con una fixture ambigua pregunta... sobre los guiones reales, ninguna seccion auxiliar entra como escena" sin mencionar preguntas). Verificado con los tres guiones reales: cero conflictos, exactamente 7/8/8 escenas sin ninguna pregunta, que es el resultado que pide el criterio de aceptacion. La pregunta queda reservada al caso donde de verdad hay dos senales que se contradicen. |
| 2026-09-01 | T-08 | La deteccion de nivel/patron ambigua (ningun encabezado del nivel esperado casa con `PATRON_ENCABEZADO_ESCENA`) y el conflicto de clasificacion de un encabezado concreto comparten la misma excepcion, `DeteccionEscenasAmbiguaError`, con una lista de `AlternativaSeparador` (nivel, patron, numero de escenas y duracion media resultantes, descripcion). La respuesta a la ambiguedad de nivel se persiste como antes de T-08 estaba previsto (`estado.SeparadorEscena.nivel`/`.patron`, T-07); la respuesta a un conflicto de clasificacion se persiste ajustando `configuracion_efectiva.secciones_auxiliares` (ya parte de `estado.json` desde T-07), no anadiendo un campo nuevo al esquema | Anadir a `estado.json` una lista de "titulos forzados a escena" especifica para resolver conflictos, o ampliar `SeparadorEscena` con ese dato | El conflicto (titulo en lista negra + rotulo de locucion en el cuerpo) es, en el fondo, una pregunta sobre si ese titulo debe seguir en la lista negra, no sobre que nivel/patron usar: `configuracion_efectiva.secciones_auxiliares` ya es el sitio correcto y ya viaja en `estado.json` desde T-07, sin tocar el esquema. Anadir un campo nuevo solo para este caso (que no se da en ninguno de los tres guiones reales) habria sido disenar por adelantado para un caso hipotetico, en contra del principio de no anadir abstraccion sin necesidad probada. `parsear_guion` revisa el conflicto en cada pasada, incluso con separador persistido, porque depende de `secciones_auxiliares`, que puede cambiar sin que cambie el separador. |
| 2026-09-01 | T-08 | Los rangos horarios de los tres guiones reales usan el guion largo Unicode U+2013 (`–`), no el guion corto ASCII: en `scripts/parser.py` ese caracter se referencia por escape (`_GUION_LARGO = "–"`) en vez de como caracter literal en el codigo fuente; en `tests/test_parser.py`, que compara contra texto real extraido de los guiones (titulos, metadatos), se anade un `per-file-ignore` de la regla `RUF001` en `pyproject.toml` en vez de escapar cada literal de prueba | Sustituir el guion largo por un guion corto ASCII en el patron (perderia la capacidad de reconocer el formato real de los tres guiones de calibracion); anadir `# noqa: RUF001` linea a linea en el test en vez de un `per-file-ignore` | La regla `RUF001` de ruff (ya seleccionada en `pyproject.toml`) marca el guion largo como caracter "ambiguo" por su parecido visual con el corto -- razonable como regla general, pero el guion largo es el caracter real que usan los guiones de produccion del dueno (verificado con `ord()`), no un error tipografico a corregir. El escape unicode resuelve el caso en codigo de produccion sin tocar la regla; el `per-file-ignore`, acotado a `tests/test_parser.py` (no a todo `tests/*`), documenta por que ese archivo concreto necesita el caracter literal sin abrir la puerta a que cualquier test futuro lo cuele sin querer. |
| 2026-09-01 | T-08 | `extraer_metadatos` no fija un esquema de claves esperadas (`Duración objetivo`, `Formato`...): extrae cualquier par `**Clave:** valor` de la cabecera y descarta las lineas con valor vacio en la misma linea | Fijar una lista de claves esperadas y avisar si falta alguna | Los tres guiones reales no usan las mismas claves: `guion-08`/`guion-09` tienen `Idea única del vídeo`, `guion-artefactos-lienzo` tiene `Promesa del vídeo` en su lugar. Fijar un esquema habria forzado a inventar una equivalencia entre claves que el dueno nunca declaro equivalentes, o habria disparado avisos falsos en guiones validos. El extractor generico cubre los tres sin necesidad de mapeo; T-12 (motor de tiempos), que sí necesita una clave concreta (`Duración objetivo`), la busca por nombre en el dict resultante. |
| 2026-09-01 | T-08 | El preambulo (texto antes del primer encabezado de cualquier nivel) nunca se trata como "escena 0": se expone integro en `ResultadoParseo.preambulo`, pero no entra en `escenas`. No hay bandera de configuracion para lo contrario | Anadir un `Configuracion.preambulo_es_escena: bool` (requisito 8 de T-08 menciona "escena 0 o ignorado, segun configuracion documentada") | En los tres guiones reales el preambulo nunca contiene locucion (solo metadatos en negrita y, a veces, el subtitulo), y `BLOQUE 0` ya existe como escena explicita dentro del cuerpo del guion: no hay ningun caso real que una bandera resolveria. Anadir una bandera sin un solo guion que la ejercite es la abstraccion prematura que el proyecto evita a proposito; la decision queda documentada aqui (y en el docstring de `parsear_guion`) en vez de en un flag sin uso, que es "configuracion documentada" en el sentido que pide el requisito 8. Revisable si un guion futuro trae locucion antes del primer `##`. |
| 2026-09-01 | T-09 | Dentro de una seccion `**LOCUCIÓN**`, el texto se particiona en tramos de cita de bloque (`> `, tipo `locucion`) y texto suelto (tipo `revisar`), fusionando lineas en blanco con el tramo contiguo (`_agrupar_cita_suelto`); el rotulo mismo es su propio bloque `no_locucion` ("marca, no se recita"), separado del cuerpo que clasifica | (a) Tratar toda la seccion `**LOCUCIÓN**` como un unico bloque `locucion` sin mirar el formato interno; (b) Incluir el texto suelto como `locucion` por estar bajo el rotulo correcto | (a) perderia exactamente el caso que el requisito 3 de T-09 pide detectar: en los tres guiones reales aparecen acotaciones de ritmo entre citas (`*(un segundo de pausa sobre el informe)*`, guion-08) y un encargo de ejemplo sin comillar dentro de una valla de codigo (guion-08 y guion-09, "Este es el que uso:" seguido de \`\`\`...\`\`\`) que NO deberian recitarse tal cual pese a estar bajo `**LOCUCIÓN**`. (b) violaria el propio requisito 3 literal ("se marca revisar"). Verificado con los tres guiones reales: cada caso de texto suelto dentro de una seccion `**LOCUCIÓN**` (las dos acotaciones de guion-08 y los dos encargos de ejemplo de guion-08/guion-09) queda en `revisar`, nunca en `locucion` ni descartado. |
| 2026-09-01 | T-09 | Señal de respaldo añadida más allá de la lista literal del requisito 2 (timestamps, acotaciones, mayúsculas...): la propia cita de bloque (`> `) cuenta como señal de inferencia de locución, incluso sin el rótulo `**LOCUCIÓN**` presente | Limitarse estrictamente a las señales enumeradas en el requisito 2 y dejar sin rótulo/sin cita → `revisar` por defecto | El propio enunciado de T-09 describe la convención real como "rótulo **LOCUCIÓN** *y* en cita de bloque": la cita no es un detalle tipográfico incidental, es la mitad de la señal contractual. Sin esta regla, el criterio de aceptación de T-09 ("con los guiones despojados de rótulos, la inferencia alcanza ≥95% de precisión en bloques de locución") sería inalcanzable con los tres guiones reales: sus secciones `**EN PANTALLA**` son prosa descriptiva llana, sin marcas de tiempo, mayúsculas ni prefijos que las distingan estructuralmente de la locución — la única señal estructural real que sobrevive a quitar los rótulos es que la locución sigue citada y `**EN PANTALLA**` nunca lo está (verificado: 0 líneas `>` fuera de una sección `**LOCUCIÓN**` o del preámbulo en los tres guiones). Con la regla, precisión medida en palabras ≥95% en los tres; sin ella, caía a ~64% en `guion-08` por las líneas en blanco heredadas de los rótulos borrados (ver la fila siguiente). |
| 2026-09-01 | T-09 | El test de precisión de la inferencia (`tests/test_clasificador.py::test_inferencia_sin_rotulos_alcanza_precision_minima_en_locucion`) mide precisión en **palabras**, no en líneas: una línea en blanco no aporta ninguna palabra recitable, así que a qué lado de un límite de párrafo caiga no debe contar ni a favor ni en contra | Medir precisión contando líneas clasificadas como `locucion` frente a líneas realmente `locucion` (más simple de escribir) | Las dos particiones (`_agrupar_cita_suelto` bajo un rótulo activo, `_dividir_en_parrafos` para texto sin rótulo) pegan las líneas en blanco al párrafo/tramo contiguo con reglas ligeramente distintas; al despojar un guión de sus rótulos, esas líneas ahora vacías (donde antes vivía `**LOCUCIÓN**`/`**EN PANTALLA**`) pueden terminar dentro de un tramo `locucion` en la versión inferida aunque en la versión original (con rótulo) cayeran fuera. Contando en líneas, eso bajaba la precisión medida de `guion-08` a 64% pese a que la clasificación del contenido recitable real era correcta — un falso negativo del propio test, no un fallo del clasificador (0 palabras mal atribuidas). Medir en palabras hace que esas líneas en blanco, que no llevan palabras, dejen de contar en ningún sentido; con esta medida, los tres guiones superan el 95% exigido. |
| 2026-09-01 | T-09 | `Configuracion` (`config.py`) gana dos campos nuevos, `rotulo_locucion: str` y `rotulos_no_locucion: tuple[str, ...]`, con los mismos valores por defecto que ya existían como constantes de módulo (`ROTULO_LOCUCION`/`ROTULOS_NO_LOCUCION`) | Seguir usando las constantes de módulo directamente desde `clasificador.py`, sin pasar por `Configuracion` | El requisito 1 de T-09 pide explícitamente "rótulos de locución y de no locución configurables"; el mismo patrón que ya usó T-08 para `secciones_auxiliares` (constante de módulo + campo espejo en `Configuracion`, congelada). Usar las constantes directamente habría dejado la palabra "configurables" sin efecto real: nada en el código habría permitido overescribirlas por ejecución sin tocar `config.py`. |
| 2026-09-01 | T-09 | `clasificador.py` re-trocea `escena.contenido`/`resultado.preambulo` (ya construidos por T-08 con `"\n".join(...)`) con `.split("\n")`, no con `.split lines()` | Usar `.splitlines()`, como hace el resto del proyecto para texto leído directamente de un archivo | `"\n".join(lineas).splitlines() != lineas` cuando la última línea de origen es una cadena vacía: `"a\nb\n".splitlines()` da `["a", "b"]`, perdiendo esa línea en blanco. Es un caso real y frecuente en los tres guiones: casi todas las escenas terminan con una línea en blanco antes del separador `---` o del siguiente encabezado (ver, p. ej., línea 54 de `guion-08-busqueda-investigacion.md`, entre el `---` de BLOQUE 0 y el encabezado de BLOQUE 1). Se detectó porque rompía la reconstrucción exacta del requisito 6 (una línea desaparecía cada vez, desplazando las líneas siguientes) antes de llegar a `test_reconstruccion_no_pierde_nada_del_guion_real`. `.split("\n")` es el inverso exacto de `"\n".join(...)` para una lista de cadenas sin `\n` interno (como son las líneas), así que no tiene este problema; se documenta en `DEVELOPERS.md` para que ningún código futuro que re-trocee un `contenido` ya ensamblado repita el mismo tropiezo. Esto NO afecta a `parser.py` (T-08): `dividir_en_bloques` trocea el `.md` original leído del archivo, no un `contenido` ya unido, así que su propio `.splitlines()` es correcto tal cual. |
| 2026-09-01 | T-10 | El subtítulo entrecomillado justo tras el título del guión (`# Título` + `## "Subtítulo"`, evidencia ya documentada en T-08) se reconoce en `convencion.py` como categoría de sección auxiliar conocida **por posición** (el primer encabezado del nivel separador de todo el guion, con título entrecomillado), no se añade a la lista negra literal `secciones_auxiliares` de `config.py` | (a) Añadir el patrón del subtítulo como una entrada más de `secciones_auxiliares`, p. ej. con un comodín; (b) No reconocerlo en absoluto y dejar que T-10 lo reporte como "sección auxiliar no reconocida" en los tres guiones reales cada vez | `secciones_auxiliares` (T-08) es una lista de **títulos fijos** (`Capítulos`, `Preparación antes de grabar`, `Notas de producción`); el subtítulo varía con cada vídeo, así que no hay título fijo que añadir sin inventar sintaxis de comodín que `parser.py` (T-08, ya completada y sin motivo para tocarla) no tiene y que sería una ampliación de alcance no pedida por T-08. La opción (b) habría hecho que `detectar_desviaciones` marcase los tres guiones reales de calibración como desviados en cada pasada por algo que T-08 ya documentó como una categoría auxiliar esperada y estable, en contra del criterio de aceptación de T-10 ("los tres guiones reales se clasifican por convención... sin recurrir a la inferencia"). Reconocerlo por posición (primer encabezado del nivel separador, con comillas) resuelve el hueco sin tocar `parser.py` ni `config.py`: verificado que dos de los tres guiones reales lo usan (`guion-08`, `guion-09`) y el tercero no lo necesita (`guion-artefactos-lienzo` pone el subtítulo en la misma línea que el `#` inicial), y en ambos casos el resultado son cero desviaciones. |
| 2026-09-01 | T-10 | `medir_consistencia_senales`/`proponer_convenciones` no tienen todavía un histórico persistido de guiones procesados entre sesiones: aceptan una `list[ResultadoClasificacion]` que quien llama construye (el guion actual, o el actual más histórico si lo tiene a mano) | Diseñar ya un almacén de histórico entre sesiones (p. ej. una lista de hashes/resultados dentro de `estado.json` o un archivo aparte) para que T-10 lo rellene y lo lea sola | No existe todavía un punto de entrada de CLI que procese "el histórico de guiones" como concepto (T-16/T-17 son quienes van a coordinar procesar más de un guion en una sesión); igual que T-02/T-04/T-05/T-07 fueron "infraestructura sin productor de datos todavía" hasta que la tarea que los usa de verdad llegó, construir aquí un almacén de histórico sería diseñar por adelantado un mecanismo de persistencia sin un solo caso real que lo ejercite. La firma `list[ResultadoClasificacion]` dice la verdad hoy (mide lo que se le pasa) y no cierra la puerta a que una sesión futura construya el histórico real y lo pase tal cual, sin cambiar esta función. |
| 2026-09-01 | T-10 | `_SENALES_CONTRACTUALES` en `convencion.py` excluye de las propuestas, además de `rotulo`/`cita_bloque`/`rotulo_no_locucion` (ya contractuales), las señales puramente estructurales `blank` (línea en blanco antes del primer rótulo de una escena) y `seccion_vacia` (sección de rótulo sin contenido) | Dejar que el mecanismo genérico de `proponer_convenciones` las tratase como cualquier otra señal de inferencia | Ambas aparecen de forma perfectamente consistente en los tres guiones reales (toda escena etiquetada empieza con una línea en blanco antes de `**LOCUCIÓN**`) pero no llevan contenido recitable ni no recitable propio que "promover a convención explícita" — proponer "adoptar la línea en blanco como convención" no es una propuesta útil para el guionista. Sin esta exclusión, el criterio de aceptación de T-10 (los tres guiones reales sin propuestas nuevas, porque ya usan la convención completa) habría fallado por una señal irrelevante, no por una desviación real. |
| 2026-09-01 | T-11 | `_refinar` (troceo por prioridad, requisito 1) trata cada nivel de prioridad como una pasada windowed: si el siguiente candidato de ese nivel no cabe dentro del máximo desde el cursor actual, usa igualmente el candidato más cercano por delante (aceptando un tramo de sobra que el siguiente nivel refina) y sigue procesando el resto del texto con el mismo nivel | (a) Cuando el candidato más cercano no cabe en el máximo, renunciar a ese nivel para *todo* el resto del texto y pasar el resto entero al siguiente nivel de prioridad (primer diseño); (b) Buscar el candidato de ese nivel más cercano al objetivo en *todo* el texto restante, no solo dentro de la ventana del máximo | (a) es un bug real, encontrado al probar contra `guion-09-proyectos.md` (no en un caso sintético): una única oración larga sin puntuación fuerte cercana al principio de un párrafo bastaba para que el nivel `FUERTE` "se rindiera" y arrastrara el resto entero del párrafo (100+ palabras) directamente a corte forzado, produciendo bloques de exactamente `palabras_por_bloque_objetivo` palabras en cascada sin usar ninguna coma, nexo o preposición disponible más adelante — verificado con un volcado del troceo real antes de corregirlo. (b) elegiría un candidato lejano que dejaría el tramo actual por encima del máximo con toda seguridad, cuando el objetivo de la ventana es precisamente encontrar el candidato que SÍ cabe; solo cuando ninguno cabe se acepta el más cercano como mal menor, exactamente lo que hace la versión elegida. |
| 2026-09-01 | T-11 | `_fusionar_bajo_minimo` no deja nunca un bloque fusionado por encima del máximo pudiendo evitarlo: si la unión de un tramo corto con su vecino más afín supera `palabras_por_bloque_max`, `_dividir_fusion` la reparte en dos usando la misma jerarquía de prioridad de corte (requisito 1) en vez de devolver la unión entera | Dejar la unión entera como un único bloque fusionado, aunque supere el máximo (primer diseño, coherente con una lectura literal de "fusiona con el vecino más afín") | Verificado contra los tres guiones reales: con la unión entera, solo el 88.6% de los 211 bloques de respiración quedaban dentro de `[6, 12]` palabras — por debajo del 90% que exige el criterio de aceptación de T-11 — porque una oración corta (p. ej. "Cuando busca, lo ves:", 4 palabras) entre dos vecinos ya cercanos al máximo no tenía forma de fundirse sin desbordarlo. Repartir la unión en dos bloques dentro de `[mínimo, máximo]` (siempre posible cuando la unión supera el máximo, porque un tramo corto es &lt; mínimo y su vecino es ≤ máximo, así que la suma es &lt; mínimo + máximo ≤ 2×máximo) sube el resultado al 100% sin tocar ni el mínimo ni el máximo configurados. |
| 2026-09-01 | T-11 | El troceo no trackea la posición palabra a palabra: cada `BloqueRespiracion` hereda `linea_inicio`/`linea_fin` del `BloqueClasificado` de origen (T-09) del que procede, no de una posición más fina | Calcular una línea aproximada por palabra dentro del bloque de origen | Ningún caso real lo necesita todavía: el reproductor (T-18+) resalta bloques de respiración dentro de una escena ya renderizada, no necesita saber en qué línea exacta del `.md` cae cada palabra. Añadir ese trackeo habría sido una precisión no pedida por el criterio de aceptación de T-11 y sin consumidor que la ejercite, igual que otras decisiones ya registradas (T-08, preámbulo; T-10, histórico) de no construir por adelantado. |
| 2026-09-01 | T-11 | `trocear_guion` reclasifica cada escena con `clasificador.clasificar_escena` en vez de recibir un `ResultadoClasificacion` (T-09) ya construido | Añadir un campo `numero_escena` a `BloqueClasificado` para poder trocear directamente sobre `ResultadoClasificacion.bloques` | `BloqueClasificado` es de T-09, ya completada y sin motivo funcional para tocarla solo por conveniencia de T-11; reclasificar escena a escena (mismo patrón que ya usa `clasificador.clasificar_guion` internamente) da acceso a `Escena.numero` sin ampliar el contrato de una tarea anterior. El coste de reclasificar es marginal (la clasificación ya es rápida, sin E/S) frente al de ampliar un dataclass ya estable. |
| 2026-09-01 | T-12 | El rango horario `(m:ss – m:ss)` de un encabezado de escena y el del metadato `**Duración objetivo:**` de cabecera reciben tratamientos distintos aunque comparten el mismo patrón (`parser.rango_segundos_titulo`, renombrada desde la privada `_rango_segundos` de T-08): el de la escena son dos marcas de tiempo del vídeo (`_duracion_objetivo_escena` resta los extremos: `3:10 − 1:55 = 75s`), el del metadato es una horquilla real de duración total (`_duracion_objetivo_metadato` la deja tal cual) | Tratar ambos como una horquilla de duraciones y usar el punto medio en los dos casos (primer diseño) | Bug real, encontrado probando contra los tres guiones reales antes de escribir un solo test: promediando los dos extremos del rango de cada escena como si fueran una horquilla de duración posible para ESA escena (en vez de restarlos), la duración objetivo de la escena 4 de `guion-08` (marcas `1:55`–`3:10`) salía como "152s de media" en lugar de sus 75s reales, e igual en las demás escenas y guiones. El ppm deducido resultante (`palabras / objetivo_inflado`) caía en 33-47 en los tres guiones reales, muy por debajo de la banda de plausibilidad `[90, 180]`, así que los tres caían siempre al respaldo con un aviso de desviación total sin sentido ("faltan 863/1625/1236 palabras"). Separar las dos semánticas (resta para la escena, horquilla tal cual para el metadato del guion completo) corrige el ppm deducido a 140-167 en los tres guiones reales -- dentro de banda, origen `deducido` -- y el aviso total pasa a ser una desviación real y pequeña (0-15%), no un artefacto del cálculo. |
| 2026-09-01 | T-12 | Duración objetivo total del guion: el metadato `**Duración objetivo:**` de cabecera si está presente (es una horquilla real); si no, un par degenerado `(suma, suma)` con la suma de las duraciones por escena, para que `ResultadoTiempos.duracion_objetivo_total_segundos` tenga siempre el mismo tipo (`tuple[int, int] \| None`) sin importar el origen | Cambiar el tipo del campo a `float \| tuple[int, int] \| None` según el origen; o ignorar el metadato y usar siempre la suma de escenas | Cambiar el tipo según el origen habría filtrado un detalle interno (de dónde salió el valor) al contrato público que cualquier consumidor futuro (T-16 a T-27) tendría que manejar con una rama extra. Ignorar el metadato tiraría información que el propio guion ya aporta explícitamente (el requisito 6 de T-12 lo nombra: "más el metadato **Duración objetivo:** de cabecera") a favor de un valor derivado; verificado que los tres guiones reales lo traen, así que no hay necesidad de descartarlo. |
| 2026-09-01 | T-12 | `_bloques_respiracion_marcados` reclasifica cada escena con `clasificador.clasificar_escena` para saber qué bloque de respiración es "fin de párrafo"/"fin de escena" (por posición, no por puntuación), en vez de añadir esos dos booleanos a `BloqueRespiracion` (T-11) | Añadir `fin_de_parrafo: bool` y `fin_de_escena: bool` a `troceo.BloqueRespiracion` y calcularlos dentro de `trocear_guion` | Mismo patrón que T-11 ya siguió con T-09 (reclasificar en vez de ampliar el dataclass de una tarea anterior ya completada): el coste de reclasificar es marginal (sin E/S) y evita que `BloqueRespiracion`, consumido también por el reproductor (T-18+) y el `.srt` (T-27) para resaltar/subtitular, cargue dos campos que solo necesita el motor de tiempos para decidir la pausa. `troceo.categoria_puntuacion_final` sí se añadió como función pública nueva (no un campo de dataclass) porque reutiliza sin duplicar los conjuntos de puntuación que ya tenía T-11 -- esa sí es una extensión sin coste, a diferencia de ampliar el dataclass. |
| 2026-09-01 | T-12 | `Configuracion.ppm_manual: int \| None` (requisito 8, calibración con toma real) no tiene mecanismo de persistencia propio: al ser un campo más de `Configuracion`, ya viaja dentro de `configuracion_efectiva` en `estado.json` (T-07) sin cambio ninguno en `estado.py` | Diseñar un campo dedicado en `EstadoProyecto` para "la calibración del dueño", distinto de `configuracion_efectiva` | No hay todavía un punto de entrada de CLI que ejerza "el dueño calibra con una toma real" como flujo real (T-16/T-17 en adelante lo tendrán); iría contra el mismo principio que ya aplicó T-10 al histórico entre sesiones y T-08 al preámbulo: no construir un mecanismo de persistencia nuevo sin un caso real que lo ejercite, cuando el ya existente (`configuracion_efectiva`) lo cubre sin tocar nada. |
| 2026-09-01 | T-13 | `normalizar_texto` resuelve las familias de regla (diccionario, moneda, porcentaje, unidad, rango, fracción, ordinal, cardinal, sigla, símbolo suelto, conjunción) en un orden de prioridad fijo, marcando cada tramo de caracteres ya resuelto en un `bytearray` de "ocupado" para que ninguna familia posterior lo reinterprete, en vez de una única pasada de regex combinada | (a) Una única expresión regular gigante con todas las alternativas a la vez; (b) Resolver solapes por posición de inicio con desempate por prioridad, sin marcar "ocupado" explícito | (a) sería ilegible y frágil de mantener (cualquier familia nueva obliga a reescribir un patrón monolítico) y no permite decidir con lógica de Python (p. ej. mirar la palabra siguiente para género/apocope) dentro de la propia coincidencia. (b) es equivalente en la mayoría de casos pero falla cuando dos familias de prioridad distinta empiezan en la misma posición con distinta longitud (p. ej. una entrada del diccionario de una palabra dentro de un rango numérico más largo que la contiene): con "ocupado" explícito, la familia de mayor prioridad siempre gana sin importar el orden de aparición por posición. Verificado con los tres guiones reales (dos casos got reales, `80%` y `SVG`) más una batería sintética que fuerza solapes entre familias. |
| 2026-09-01 | T-13 | Apócope de "uno"/"veintiuno" y concordancia de género en la lectura de un cardinal (requisito 2) se activan SOLO si, en el texto de entrada, el número va seguido inmediatamente de una palabra alfabética (candidato a sustantivo); sin nada detrás, se lee la forma plena sin apocopar (`"21"` a final de frase → "veintiuno", no "veintiún") | Apocopar siempre que el dígito final sea 1, sin mirar el contexto | La apócope en español es una propiedad de la función adjetiva del numeral ("veintiún euros"), no del numeral en sí ("leímos el veintiuno", "capítulo veintiuno"): apocopar siempre habría producido lecturas incorrectas en años sueltos, numeración de pasos o cualquier cardinal que no precede a un sustantivo — exactamente el caso más común en un guión de producción (años, referencias, recuentos). Mirar la palabra siguiente es una heurística de una sola palabra de contexto, no un analizador sintáctico, pero cubre el caso real (`"21 alumnos"` → "veintiún alumnos") sin inventar apócopes donde no toca. |
| 2026-09-01 | T-13 | La concordancia de género de un sustantivo detectado por contexto usa una heurística de sufijo (termina en "-a"/"-ción"/"-dad"... → femenino; si no, masculino) con una lista corta de excepciones frecuentes (`día`, `mano`, `foto`...), no un analizador morfológico real ni una lista exhaustiva del español | (a) Lista exhaustiva de sustantivos españoles con su género (miles de entradas, mantenimiento continuo); (b) No intentar concordancia de género en absoluto, leer siempre en masculino | (a) es una inversión desproporcionada para el caso de uso real (normalizar cifras de un guión de producción, no traducir literatura): el criterio de aceptación de T-13 no la exige, y el propio requisito 3 (diccionario del dueño con prioridad sobre toda regla automática) es la vía prevista para corregir cualquier fallo puntual sin tocar código — el dueño añade `"1 vez": "una vez"` si la heurística falla, igual que ya hace con siglas o cifras con lectura no estándar. (b) perdería casos reales y verificables como `"1 hora"` → "una hora", que el propio requisito 2 pide explícitamente ("concordancia de género y número"). La heurística elegida acierta en los sustantivos más frecuentes de un guión de producción (horas, personas, veces, páginas, semanas...) y su fallo nunca rompe nada: en el peor caso, propone una forma masculina donde tocaba femenina, y el dueño la corrige en el diccionario o rechazándola cuando exista el aparato de aceptar/rechazar de T-15. |
| 2026-09-01 | T-13 | Las tablas de valores por defecto nuevas de `config.py` (`SIMBOLOS_MONEDA`, `UNIDADES_ABREVIADAS`) son diccionarios planos de módulo, NO se espejan como campos de `Configuracion` (a diferencia de `rotulo_locucion`/`rotulos_no_locucion` en T-09, o `ppm_respaldo`/etc. desde T-12) | Añadir `simbolos_moneda`/`unidades_abreviadas` como campos de `Configuracion`, con tuplas de pares para mantener la inmutabilidad del dataclass congelado (mismo patrón que `secciones_auxiliares: tuple[str, ...]`) | La regla de §0.2 ("todo valor por defecto... es sobreescribible por el dueño") ya queda satisfecha por un camino distinto y más directo para este caso concreto: el diccionario de excepciones (`diccionario-locucion.json`, requisito 3) tiene prioridad sobre CUALQUIER regla automática, entrada a entrada — el dueño ya puede sobreescribir "€" o "km" con una entrada literal sin que `Configuracion` intervenga. Espejarlas además como campos habría exigido convertir dos `dict[str, ...]` en `tuple[tuple[str, ...], ...]` solo por la restricción de hashabilidad del dataclass congelado, una complicación de tipos sin un caso de uso real que la ejerza (mismo principio que ya aplicó T-10 al histórico y T-12 a la calibración: no construir mecanismo nuevo sin necesidad probada). Revisable si una tarea futura necesita sobreescribir la tabla entera de una vez, no entrada a entrada. |
| 2026-09-01 | T-13 | `normalizar_guion` recibe `list[BloqueRespiracion]` (la salida de `trocear_guion`, T-11) y normaliza el texto YA trozado, no el texto de locución antes de trocear | Normalizar antes del troceo (sobre `BloqueClasificado` de T-09) y dejar que T-11 trocee el texto ya normalizado | El propio requisito 2 de T-11 ya anticipa el orden correcto: "nunca cortar... una expresión normalizada por T-13", lo que exige que el troceador conozca los límites de una normalización futura o que la normalización ocurra después, sobre bloques cuyos límites ya son estables. Normalizar antes arriesgaría que T-11 corte a mitad de una propuesta ("mil quinientos" partido en dos bloques de respiración); normalizar después opera sobre texto cuyos límites de bloque nunca van a cambiar por esta tarea, y cada bloque se normaliza de forma independiente sin necesidad de recalcular offsets entre escenas. |
| 2026-09-01 | T-14 | `deteccion.py` opera sobre `BloqueRespiracion` (T-11) ya trozado -- la misma unidad que usa `normalizacion.py` (T-13) -- en vez de sobre el texto de locución completo de la escena antes de trocear (`BloqueClasificado`, T-09) | Detectar "frase sin punto de respiración" como una oración completa (delimitada por puntuación fuerte) sobre el texto de locución sin trocear, reimplementando la partición de oraciones que ya hace `troceo.py` | El enunciado de T-14 fija la dependencia en T-11, no en T-09, y el propio requisito 1 ("frase sin punto de respiración... afecta al troceo") describe un problema que ya es visible a la granularidad del bloque de respiración: un bloque de 6-12 palabras sin ninguna coma ni guion dentro es exactamente "no hay donde respirar". Reimplementar la partición de oraciones habría duplicado la lógica de prioridad de corte de T-11 (fuerte→débil→nexos→sintagma) solo para volver a llegar al mismo sitio. Operar sobre `BloqueRespiracion` también da acceso directo a `numero_escena`/`linea_inicio`/`linea_fin` para localizar el aviso (requisito 6), sin recalcular esa correspondencia. |
| 2026-09-01 | T-14 | Solo la familia `sin_punto_respiracion` marca `admite_particion=True` y adjunta `particion_sugerida` (una tupla con las dos mitades del texto, partiendo por el nexo subordinante más cercano al centro del bloque o, si no hay ninguno, por el centro exacto en palabras); las otras cuatro familias (`cacofonia`, `trabalenguas`, `anglicismo`, `estructura_dificil`) nunca rellenan esos dos campos | Dar a las cinco familias la misma capacidad de proponer partición, dejando que una tarea futura (T-15) decida cuáles usar | El requisito 6 de T-14 es explícito y literal: "estos avisos nunca generan reescritura... la única excepción es la frase sin punto de respiración, que sí puede proponer partición porque afecta al troceo". Ampliar la capacidad a las demás familias habría contradicho una decisión ya tomada por el dueño y promovida a §0.2 ("alcance de las reescrituras: solo forma dicha y respiración"). `particion_sugerida` es una propuesta de datos, no una reescritura aplicada: sigue siendo T-15 quien decide si materializarla. |
| 2026-09-01 | T-14 | Las cinco familias se detectan con heurísticas de caracteres (prefijo/sufijo compartido para sílaba/rima, longitud de palabra y grupos de consonantes para trabalenguas, listas de palabras para subordinantes/negaciones/anglicismos, conteo de marcas para incisos), sin un analizador sintáctico o fonético real del español, y sin batería de excepciones tipo `diccionario-locucion.json` (T-13) | Escribir un analizador sintáctico real (dependencias, categorías gramaticales) para detectar voz pasiva/subordinadas con precisión; o exigir una lista exhaustiva de anglicismos y palabras difíciles | Mismo principio que T-13 aplicó a la concordancia de género (`_genero_por_sustantivo`): un analizador real es una inversión desproporcionada para el caso de uso (avisar en un guion de producción, no procesar lenguaje natural en general), y el criterio de aceptación de T-14 solo exige "un test que la detecta y un contraejemplo que no dispara falso positivo" por familia, no precisión lingüística exhaustiva. A diferencia de T-13, T-14 no reescribe nada (salvo la excepción de arriba): un falso positivo es solo un aviso de más que el dueño ignora a mano, nunca un texto incorrecto escrito en su lugar, así que el coste de una heurística imperfecta es mucho menor aquí. Verificado contra los tres guiones reales: ninguna familia dispara un volumen desbordado de avisos (15-17 por guion sobre 71-87 bloques) ni deja de disparar en absoluto. |
| 2026-09-01 | T-14 | `ANGLICISMOS_COMUNES` en `config.py` es un diccionario plano de módulo, no un campo de `Configuracion` (mismo patrón que `SIMBOLOS_MONEDA`/`UNIDADES_ABREVIADAS` de T-13); el resto de umbrales nuevos (longitudes, ventanas, mínimos de repetición) sí son campos de `Configuracion`, con validación de entero positivo en `__post_init__` | Espejar también `ANGLICISMOS_COMUNES` como campo de `Configuracion`, con tuplas de pares por la restricción de hashabilidad del dataclass congelado | Igual que en T-13: es una tabla completa sin un mecanismo de sobreescritura entrada a entrada ya existente que la haga innecesaria (T-14 no tiene un diccionario de excepciones propio, a diferencia de T-13), pero tampoco hay un caso real todavía que necesite sobreescribir toda la tabla de una vez -- ampliarla ahora sería la misma abstracción prematura que T-10/T-12/T-13 ya evitaron. Los umbrales numéricos sí son campos de `Configuracion` porque la regla "sin números mágicos" (§0.2) exige que sean sobreescribibles, y un entero suelto no tiene el problema de hashabilidad de un diccionario. |
| 2026-09-01 | T-15 | `Reescritura.id` es un hash (sha256 truncado a 16 hex) de `(numero_escena, linea_inicio, linea_fin, familia, inicio, fin, original)` -- la ocasión concreta -- en vez de incluir la `propuesta` en la clave, o de usar un contador/UUID aleatorio | (a) Incluir también `propuesta` en la clave del hash; (b) Identificador aleatorio (UUID) generado al proponer, guardado aparte para relacionarlo con la ocasión | El requisito 4 exige que revalidar no vuelva a proponer una reescritura "ya decidida": si una regla de T-13 mejora y cambia la forma dicha que propone para el mismo número exacto en el mismo sitio, sigue siendo la misma ocasión para el dueño ("ya decidí qué hacer con este 2026 de aquí"), así que la propuesta no debe entrar en la identidad. (a) rompería esa continuidad en cuanto una heurística cambiara de resultado. (b) es equivalente en identidad pero no es determinista entre pasadas: dos ejecuciones sobre el mismo guion sin cambios generarían IDs distintos si no se persistiera aparte una tabla de correspondencia, exactamente el mecanismo de persistencia nuevo sin necesidad real que T-10/T-12/T-13 ya evitaron. Un hash determinista de los datos que ya existen no necesita ese almacén. |
| 2026-09-01 | T-15 | Solo la familia `sin_punto_respiracion` de T-14 (con `admite_particion=True`) genera una `Reescritura`, bajo la familia nueva `particion_respiracion`; las otras cuatro familias de T-14 (cacofonía, trabalenguas, anglicismo, estructura difícil) nunca entran en `recopilar_propuestas`, aunque `ResultadoDeteccionBloque` se le pase entero | Aceptar un parámetro de "familias permitidas" configurable, para que una sesión futura pueda ampliar qué avisos se convierten en reescrituras sin tocar el código | El alcance de las reescrituras ("solo forma dicha y respiración") es una decisión del dueño promovida a §0.2, no un parámetro de ejecución: dejarlo configurable habría abierto una puerta que el propio protocolo dice que solo el dueño puede abrir ("ampliar este alcance es decisión del dueño, no una P-XX"). Si el dueño amplía el alcance algún día, es un cambio de código explícito en `_reescritura_desde_particion`, no un flag que una ejecución cualquiera pueda activar sin querer. |
| 2026-09-01 | T-15 | El formato marcado (`formatear_reescritura`) es un bloque de texto independiente (delimitado por `<!-- reescritura id=... -->`/`<!-- /reescritura -->`), pensado para insertarse dentro de una escena de `guion-escenas.md`, no el documento completo -- T-16, que construye ese documento, todavía no existe | Esperar a T-16 para diseñar el formato marcado, ya que el requisito 1 de T-15 habla del ".md anotado" que en sentido estricto es una tarea posterior | El orden de §1 de SEGUIMIENTO fija T-15 antes de T-16 con dependencia solo en T-13/T-14, no en T-16; mismo patrón que T-02/T-04/T-05/T-07 (infraestructura sin productor/consumidor final todavía). Diseñar el bloque marcado como una pieza autónoma, testeable con el ciclo completo del criterio de aceptación de T-15 sin necesitar el documento entero, evita invertir el orden de la hoja de ruta o bloquear T-15 en una tarea que no le toca todavía. T-16 consume `formatear_reescritura`/`extraer_decisiones` tal cual cuando llegue. |
| 2026-09-01 | T-15 | La marca de decisión es una única palabra en mayúsculas (`PENDIENTE`/`ACEPTAR`/`RECHAZAR`) que sirve a la vez de estado mostrado y de campo editable -- el dueño la sobrescribe con la palabra que quiere -- leída con `Decisi[oó]n\W*(ACEPTAR|RECHAZAR|PENDIENTE)` (cualquier no-palabra entre "Decisión" y la marca, insensible a mayúsculas) | Un par de casillas tipo Markdown (`- [ ] Aceptar` / `- [ ] Rechazar`) al estilo checklist de GitHub | El requisito 2 pide explícitamente "una marca simple, sin sintaxis frágil". Dos casillas exigen que el dueño marque una y deje la otra vacía (dos ediciones posibles, un estado inconsistente si marca las dos o ninguna); una sola palabra reemplazable es una única edición con tres estados mutuamente excluyentes por construcción. La tolerancia del regex a `:`, `**` (negrita Markdown) y espacios de más viene de un caso real detectado al escribir el test: la marca vive dentro de una línea `> **Decisión:** PENDIENTE`, y un regex que exigiera un `:` literal justo antes de la palabra fallaba contra el propio formato que el modulo genera. |
| 2026-09-01 | T-15 | Materializar una partición aceptada (`aplicar_particion_aceptada`) sustituye el `BloqueRespiracion` por los dos resultantes buscándolo por igualdad exacta de `(numero_escena, linea_inicio, linea_fin, texto)`, y ambos bloques nuevos heredan `corte_forzado` del bloque de origen sin recalcularlo | Recalcular `corte_forzado` para cada mitad ejecutando de nuevo el algoritmo de troceo de T-11 (`trocear_texto`) sobre cada mitad | El campo `corte_forzado` describe una propiedad del algoritmo de corte de T-11 (si tuvo que forzar un corte sin puntuación disponible); esta partición es una decisión explícita del dueño que no pasa por ese algoritmo, así que no hay un valor "correcto" que recalcular sin inventarle un significado nuevo al campo. Ningún consumidor existe todavía (T-16+) que dependa del valor exacto tras una partición aceptada, mismo principio que T-11 y T-12 ya aplicaron a otros campos derivados sin caso de uso real que fuerce la precisión. Revisable si un consumidor futuro sí distingue el comportamiento por `corte_forzado` tras una partición manual. |
| 2026-09-01 | T-16 | Dentro de cada escena de `guion-escenas.md`, el texto se reorganiza en dos secciones nuevas -- "Locución" (solo los bloques de respiración recitables, numerados) y "Indicaciones no recitables" al pie -- en vez de reproducir tal cual la estructura original intercalada `**LOCUCIÓN**`/`**EN PANTALLA**`/`**NOTA**` del guion de origen | Reproducir literalmente la convención de rótulos del guion de origen dentro del documento de revisión, para que en teoría `parser.parsear_guion` pudiera releerlo sin cambios | T-16 solo exige que el documento sea "legible en un editor de texto plano" y "reprocesable por T-17"; no exige que T-17 tenga que ser literalmente el mismo `parser.py`/`clasificador.py` del guion de origen, y esa tarea (todavía sin especificar en detalle) es libre de definir su propio mecanismo de relectura. Forzar la reutilización literal de la convención habría acoplado el diseño de T-16 a una suposición sobre T-17 que ningún requisito confirma, y habría estorbado la lectura de una sola sentada: separar "lo que se dice" de "lo que no se recita" es más legible que intercalarlo como en el guion de producción, que está pensado para escribirse, no para revisarse de un vistazo. |
| 2026-09-01 | T-16 | Cada bloque de respiración se delimita con un ancla propia `<!-- bloque escena=N indice=K --> ... <!-- /bloque -->` (numeración 1-based por escena), leída de vuelta por `extraer_texto_bloques` | (a) No anclar nada y confiar en que T-17 localice el texto editado por diferencia textual (diff) contra el guion original; (b) Reutilizar como ancla el rango de líneas de origen (`linea_inicio`/`linea_fin`) sin un índice adicional | (a) es exactamente el "mecanismo de persistencia/relectura nuevo sin caso de uso real" que T-10/T-12/T-13/T-15 ya evitaron -- diseñarlo a ciegas para una T-17 todavía sin especificar en detalle sería adivinar sus requisitos. (b) no es único: varios bloques de respiración de una misma escena pueden compartir `linea_inicio`/`linea_fin` porque T-11 trocea un mismo párrafo de origen en varios bloques y no trackea posición más fina (mismo motivo que ya obligó a T-15 a comprobar `texto[inicio:fin] == original` en vez de fiarse solo del rango de líneas). Un ancla con índice propio, misma idea que `<!-- reescritura id=... -->` de T-15, es inequívoca dentro de este documento y no presupone cómo resolverá T-17 el resto del ciclo de revalidación. |
| 2026-09-01 | T-16 | Un aviso de la familia `sin_punto_respiracion` con `admite_particion=True` no se muestra como aviso plano junto a su bloque: se comprueba justo ese flag (no la presencia de una `Reescritura` ya aceptada/rechazada) para decidir que ya se mostró como la `Reescritura` de familia `particion_respiracion` correspondiente | Comprobar en su lugar si existe una `Reescritura` de familia `particion_respiracion` para ese bloque entre las recibidas, y ocultar el aviso solo si aparece | `_detectar_sin_punto_respiracion` (T-14) es determinista: siempre que dispara con `admite_particion=True` rellena también `particion_sugerida`, y `_reescritura_desde_particion` (T-15) siempre convierte ese aviso concreto en una `Reescritura` cuando `recopilar_propuestas` recibe el mismo `ResultadoDeteccionBloque`. Comprobar el flag del propio aviso evita depender de que quien llama a `generar_documento_revision` le haya pasado exactamente la lista de reescrituras derivada de esas mismas detecciones -- una futura llamada con listas construidas por separado (p. ej. tests unitarios de otras piezas) seguiría ocultando el aviso correcto sin tener que reconstruir la reescritura solo para comprobar que existe. |
| 2026-09-01 | T-16 | El título mostrado en la cabecera del documento (`# Guion de revisión — <nombre_guion>`) lo decide quien llama a `generar_documento_revision` (parámetro `nombre_guion: str = "guion"`), no se infiere del `ResultadoParseo` | Inferir el título de `resultado.metadatos` o de la primera `SeccionAuxiliar` de nivel 1 (el encabezado `#` inicial del guion) | Los tres guiones reales no tienen una clave de metadato de título consistente (`extraer_metadatos` solo captura pares `**Clave:** valor`, y el título vive en el propio encabezado `#`/`##` inicial, con formas distintas entre los tres); `SeccionAuxiliar` tampoco guarda su nivel de encabezado, así que distinguir "es el título" de "es cualquier otro encabezado auxiliar de nivel 1" exigiría una heurística frágil sin caso de uso que la justifique todavía. Quien orquesta la generación (la futura CLI de T-30) ya conoce la ruta del guion y puede decidir el título con la información que tenga a mano (nombre de archivo, metadato, o preguntarle al dueño), sin que este módulo tenga que adivinarlo. |
| 2026-09-01 | T-16 | `guardar_documento_revision` copia el archivo previo a `<nombre>.bak-<marca_de_tiempo>` antes de sobrescribirlo si ya existía, pero no usa la escritura atómica de fichero-temporal-más-reemplazo de `estado.guardar_estado` (T-07) | Aplicar el mismo patrón de escritura atómica que `estado.json` | La invariante (d) de §0.2 ("sin borrado destructivo") exige la copia de seguridad, que ya está cubierta; no exige que un corte a mitad de escritura sea imposible de por sí, a diferencia de `estado.json`, que es el único archivo que el proceso relee automáticamente en cada pasada sin intervención del dueño y cuya corrupción bloquearía toda sesión futura. `guion-escenas.md` es un documento que el dueño edita a mano y siempre puede regenerarse desde `estado.json` si un corte dejara una escritura a medias; la copia `.bak` ya resuelve el caso que de verdad importa aquí (no perder la versión anterior, editada a mano). Revisable si T-17 encuentra un caso real de corte a mitad de escritura que rompa una sesión de revisión. |
| 2026-09-01 | T-17 | `tiempos.calcular_tiempos` se divide en dos: `bloques_respiracion_marcados` (renombrada desde `_bloques_respiracion_marcados`, ahora pública) construye los bloques de una escena por reclasificación; el nuevo `calcular_tiempos_desde_marcados` recibe ya ese `dict[int, list[tuple[BloqueRespiracion, bool, bool]]]` como parámetro y hace el resto del cálculo (ritmo, pausas, agregados). `calcular_tiempos` pasa a ser un envoltorio de dos líneas que arma el dict por reclasificación y delega | (a) Añadir a `tiempos.py` una función paralela para T-17 que duplicara la lógica de ritmo/pausas/agregados; (b) Cambiar la firma de `calcular_tiempos` para aceptar una lista de bloques opcional | T-12 fija `calcular_tiempos` como "la única fuente de tiempos" (requisito 4) precisamente para que nadie recalcule por su cuenta; pero su única forma de obtener bloques era reclasificar el guion de origen, y T-17 necesita tiempos sobre bloques ya materializados (particiones aceptadas) y con texto editado a mano superpuesto -- un caso que `ResultadoParseo` por sí solo no describe. (a) rompería esa unicidad de facto (dos funciones calculando lo mismo con lógica copiada). (b) ensuciaría la firma de la función más usada del módulo (T-16, futura CLI T-30) con un parámetro que casi nadie pasa. Extraer el núcleo deja `calcular_tiempos` con el mismo comportamiento exacto (ninguna de las ~180 aserciones de `tests/test_tiempos.py` se tocó) y da a T-17 el único punto de entrada que necesita sin duplicar una sola línea de la lógica de ritmo. |
| 2026-09-01 | T-17 | `reescrituras.SEPARADOR_MITADES` pasa de privada (`_SEPARADOR_MITADES`) a pública, sin cambiar su valor (`"\n\n"`) | Duplicar el separador como constante propia de `revalidacion.py` | `revalidacion.py` necesita partir `Reescritura.propuesta` en sus dos mitades para materializar una partición aceptada con la misma identidad exacta que ya usa `reescrituras.aplicar_particion_aceptada` -- duplicar el separador como cadena literal en dos módulos es exactamente el acoplamiento implícito que una constante existe para evitar; si algún día cambia el formato de una `propuesta` de partición, un solo sitio deja de estar sincronizado en vez de dos. |
| 2026-09-01 | T-17 | Identidad de un bloque de respiración entre pasadas de revalidación: `(numero_escena, indice_original, mitad)`, donde `indice_original` es la posición 0-based del bloque dentro de la reclasificación fresca de la escena (antes de cualquier partición) y `mitad` es `None`/`'a'`/`'b'`. NO se usa el número de ancla (`escena=N indice=K`) que ve `guion-escenas.md`, ni `(numero_escena, linea_inicio, linea_fin)` a secas | (a) Usar el número de ancla del documento como identidad; (b) Usar `(numero_escena, linea_inicio, linea_fin)` sin más | (a) se rompe en cuanto una partición se acepta EN LA MISMA revalidación que trae la aceptación: el número de bloques de esa escena crece de golpe, así que el ancla K de un bloque posterior en la misma escena deja de apuntar al mismo contenido que apuntaba en el documento leído. (b) no es única: T-11 (troceo) ya trocea un mismo párrafo en varios bloques de respiración que comparten `linea_inicio`/`linea_fin` (decisión de T-11, "no trackea posición más fina que el párrafo de origen"), así que dos bloques de respiración distintos de un mismo párrafo colisionarían. `indice_original` es estable porque `trocear_texto` es determinista sobre el mismo texto de entrada (el `.md` de origen, que T-17 no toca) y no depende de si otra escena o un bloque anterior de la MISMA escena se partió o no; una partición solo inserta una ramificación (`'a'`/`'b'`) en el índice del bloque que se parte, sin desplazar los índices de los demás. |
| 2026-09-01 | T-17 | Una edición manual se detecta comparando, ancla a ancla, el texto que trae el `guion-escenas.md` leído contra el texto que el sistema derivaría para ese mismo bloque con las decisiones YA GUARDADAS en `estado.json` al empezar la revalidación (antes de leer las marcas nuevas del propio documento) -- no contra un registro explícito de "esto lo tocó el dueño" | Guardar en `estado.json` un campo aparte que marque qué bloques fueron editados a mano, actualizado cada vez que se detecta una edición | Añadir un registro explícito de "bloques editados" es el mecanismo de persistencia nuevo sin necesidad probada que T-10/T-12/T-13 ya evitaron: comparar contra el texto derivado ya responde la única pregunta que hace falta ("¿esto es distinto de lo que el sistema habría puesto ahí?") sin guardar nada adicional, y es correcto por construcción -- el texto derivado se recalcula siempre desde el guion de origen (inmutable) más las decisiones persistidas, así que la comparación da el mismo resultado en cualquier pasada futura mientras el dueño no vuelva a tocar ese bloque. |
| 2026-09-01 | T-17 | Límite de alcance aceptado, no resuelto: si un mismo `BloqueRespiracion` de origen tiene a la vez una normalización (T-13) aceptada y una partición (T-14/T-15) aceptada, la normalización no se materializa en ninguna de las dos mitades resultantes -- sigue registrada como `aceptada` en `estado.json` (invariante (b), nada se pierde), pero el texto final no la aplica | Recalcular los offsets de la normalización sobre cada mitad tras partir el bloque | Los offsets de una `Normalizacion`/`Reescritura` de familia distinta de partición se calculan sobre el bloque ENTERO (T-13); tras partirlo, `texto[inicio:fin]` de cualquiera de las dos mitades ya no corresponde a ese tramo, y el guardián `bloque.texto[inicio:fin] == original` (mismo patrón que ya usa `documento_revision._reescrituras_de_bloque` desde T-16) lo excluye correctamente en vez de aplicarlo mal. Recalcular offsets exigiría mapear un tramo del texto original a su mitad correspondiente -- una traducción de coordenadas que ningún caso real (ninguno de los tres guiones de calibración lo provoca) justifica todavía construir; T-14 ya limitó expresamente qué familia admite partición (solo `sin_punto_respiracion`), así que este cruce concreto (partición + normalización sobre el mismo bloque) es infrecuente por diseño. Revisable si un guion real lo produce con normalizaciones perdidas de forma visible para el dueño. |
| 2026-09-01 | T-17 | `estado.validacion` (contenedor genérico reservado desde T-07, sin cambio de esquema ni migración) guarda `{"historial": [...], "ultima": {...}}`: un registro `{marca_tiempo, validado, incidencias}` por cada llamada a `revalidar_guion`, más un acceso directo al último | Sobrescribir un único registro sin historial (mismo patrón de "no guardar más de lo que hace falta" que T-10/T-12/T-13 ya aplicaron a otros contenedores) | A diferencia de los casos de T-10/T-12/T-13, aquí el propio requisito 4 de T-17 pide explícitamente "cada revalidación... se registra con marca de tiempo": un único campo sobrescrito no cumple el "cada" literal. El historial es una lista simple sin límite de tamaño propio (un proyecto de guion no acumula miles de revalidaciones), así que no hace falta ninguna poda todavía; se deja anotado por si una sesión futura la necesita. |
| 2026-09-01 | T-17 | `revalidar_guion` no regenera ni guarda `guion-escenas.md` por su cuenta, ni llama a `estado.guardar_estado`: deja `estado.reescrituras`/`estado.validacion` listos en memoria (mutando el objeto `EstadoProyecto` recibido) y devuelve `ResultadoRevalidacion` para que quien orqueste la sesión decida cuándo persistir a disco y cuándo regenerar el documento con `documento_revision.generar_documento_revision` | Que `revalidar_guion` hiciera el ciclo completo (leer, recalcular, regenerar, guardar estado y documento) | Mismo patrón que T-07/T-10/T-12/T-13 ya siguieron: no existe todavía una CLI real (T-30) que orqueste una sesión completa contra archivos en disco, así que decidir aquí cuándo escribir sería adivinar esa orquestación futura. Mantener `revalidar_guion` como una función pura sobre datos en memoria (recibe `resultado`/`texto_documento`/`estado`, devuelve un resultado) es lo que ya prueba `tests/test_revalidacion.py` end-to-end sin tocar el disco salvo en el propio `tmp_path` de cada test, exactamente como T-16 dejó `generar_documento_revision`/`guardar_documento_revision` como dos pasos separados en vez de uno solo. |
| 2026-09-01 | T-18 | Los datos (escenas, bloques, tiempos) viajan al reproductor como JSON dentro de un `<script type="application/json" id="datos-reproductor">`, leído con `JSON.parse` y volcado al DOM solo con `textContent` en `guion.js`; nunca se interpola el texto del guion directamente en el marcado HTML ni se usa `innerHTML` | Renderizar el HTML de cada escena/bloque directamente en Python (f-strings) con `html.escape` por campo | Con datos como JSON, el único punto de escape server-side que importa es el que evita que el JSON cierre su propio `<script>` (ver fila siguiente); todo lo demás lo resuelve `JSON.parse` + `textContent`, que nunca interpreta marcado, por construcción, aunque el guion contuviera literalmente `<script>` o `onerror=`. Renderizar HTML por campo exigiría mantener `html.escape` correcto en cada punto de inserción para siempre (T-19 a T-26 añaden más), un riesgo mayor que centralizarlo una vez en el borde de serialización. Verificado contra un guion hostil con `</script><script>alert(1)</script>` y `<img onerror=...>`: se renderiza como texto literal, sin diálogos ni errores de consola (Chromium vía Playwright, verificación manual de esta sesión, no una dependencia del proyecto). |
| 2026-09-01 | T-18 | `_json_seguro_para_script` sustituye los caracteres `<`, `>` y `&` por su escape Unicode de seis caracteres cada uno (la secuencia "barra invertida, u, cero, cero, tres, c" para `<`, y análogas para `>` y `&`) DESPUÉS de `json.dumps(datos, ensure_ascii=False)`, en vez de usar `ensure_ascii=True` o el `HTMLParser`/`markupsafe` de la biblioteca estándar | (a) `ensure_ascii=True` (escapa todo no-ASCII, incluidas las tildes); (b) no escapar nada y confiar solo en `textContent` | (a) resuelve el mismo problema pero de más, mezclando dos preocupaciones distintas: "no cerrar el `<script>`" es un problema de HTML, "las tildes se leen mal" no existe si el documento declara `charset="utf-8"` (que declara). Escapar solo los tres caracteres que de verdad pueden cerrar una etiqueta deja el JSON legible para quien inspeccione el `.html` a mano, que es justo el público de un archivo pensado para abrirse con doble clic. (b) es la defensa de `guion.js` (fila anterior) pero no cubre el caso de abrir el `.html` con un lector de texto plano o un depurador que muestre el DOM sin ejecutar JS: el JSON en sí debe ser inerte como HTML, no solo el render posterior. Las dos defensas son independientes a propósito (defensa en profundidad), no redundantes. |
| 2026-09-01 | T-18 | El generador embebe datos, CSS y JS en un único `.html` a partir de tres archivos de plantilla en `assets/reproductor/` (`plantilla.html`, `estilo.css`, `guion.js`) unidos por sustitución de marcadores (`__ESTILO__`, `__SCRIPT__`, etc.), no con f-strings de Python que construyan el marcado línea a línea | Escribir el HTML/CSS/JS como cadenas Python dentro de `reproductor.py` | El requisito 1 de T-18 pide explícitamente plantillas en `assets/`. Además, HTML/CSS/JS como archivos de verdad son legibles y editables con las herramientas normales de cada lenguaje (resaltado de sintaxis, linters de editor), y separan con claridad qué es "diseño de la página" (assets/) de qué es "orquestación de datos" (`reproductor.py`), mismo criterio de separación que ya aplican `references/marca-480.md` para la guía de marca frente al código que la usa. |
| 2026-09-01 | T-18 | `verificar_salidas.py --fixture` genera el reproductor sobre el primer guion de `fixtures/reales/` (orden alfabético) en vez de esperar a `fixtures/guion-ejemplo.md` (T-32) o a la canalización completa de salidas (T-30) | Dejar "Generación del reproductor"/"Auto-contención" en NO APLICABLE hasta que T-30 y T-32 existan | El propio T-00 (fila de esta tabla, 2026-08-31) ya anticipó este momento exacto: "cuando T-18 genere el HTML, la comprobación de auto-contención deja de ser NO APLICABLE sin tocar el protocolo". Esperar a T-30/T-32 dejaría la regla dura de auto-contención (§0.2) sin verificación real durante todo el resto de FASE B4 (T-19 a T-26), justo el tramo donde más cambia el HTML generado. Usar un guion real de calibración ya versionado es sustituible sin ceremonia cuando T-32 aporte el guion de ejemplo oficial: basta con que `fixtures/guion-ejemplo.md` exista y `CARPETA_GUIONES_REALES` cambie de carpeta, sin tocar la lógica de la etapa. |
| 2026-09-02 | T-19 | El "estado" por escena del índice (`pendiente`/`grabada`/`revisada`, requisito 1) vive solo en memoria de la pestaña (`estadosEscena` en `guion.js`): toda escena arranca en `pendiente` y pasa a `grabada` en cuanto se ha reproducido y cerrado al menos una vez; `revisada` queda definida en la tabla de etiquetas pero ninguna interacción de T-19 la activa todavía. No se persiste entre recargas | (a) Persistir el estado ya en `localStorage` desde T-19; (b) Escribir el estado de vuelta a `estado.json` desde el navegador | (a) es exactamente el mecanismo que T-26 tiene que diseñar con su propio criterio de aceptación (aviso si no persiste, exportar/importar) — adelantarlo aquí a ciegas duplicaría trabajo y podría contradecir esa tarea. (b) es imposible sin servidor: el reproductor es un `.html` abierto con doble clic desde `file://`, sin proceso Python detrás una vez generado. R-02 ya tiene registrado en `ROADMAP_PRODUCTO.md` que "T-19 muestra un estado por escena pero nada registra qué pasó en cada toma": esta fila deja constancia de que ese "nada registra" es deliberado y transitorio, no un olvido. |
| 2026-09-02 | T-19 | Cada fila del índice es un único `<button>` (`escena-fila-N`) que hace de fila navegable Y de botón de reproducir a la vez, en vez de dos elementos interactivos separados (una fila informativa más un botón de play aparte) | Fila no interactiva (`<li>` o `<div>`) con un botón de play independiente dentro | El requisito 1 (navegable con flechas/Tab/Enter/clic) y el requisito 2 (botón de play por escena) describen el mismo gesto del usuario: seleccionar una escena y arrancarla. Duplicar el elemento habría exigido sincronizar el foco entre la fila y su botón interno sin ganar nada — la fila entera ya es un `<button>` nativo, focalizable por `Tab`, activable con `Enter`/`Espacio` y con clic sin JS adicional. |
| 2026-09-02 | T-19 | `solicitarPantallaCompleta` refoca el botón "Volver al índice" dentro del `.then()` de la promesa de `requestFullscreen()`, no de forma síncrona antes de pedir pantalla completa | Llamar a `botonVolver.focus()` una sola vez, antes de `requestFullscreen()` | Verificado con Chromium (mismo navegador que abrirá el reproductor, via Playwright headless, comprobación manual de esta sesión, no dependencia del proyecto): al completar la transición a pantalla completa, Chromium vacía el foco de la página (`document.activeElement` pasa a `<body>`), pisando cualquier `.focus()` anterior. Sin este ajuste, el recorrido de teclado del criterio de aceptación de T-19 se queda sin foco visible justo al entrar en la escena y exige una pulsación de `Tab` "a ciegas" que el criterio no contempla. Refocar tras resolverse la promesa hace que el foco esté siempre en un elemento visible y conocido nada más terminar la transición. |
| 2026-09-02 | T-20 | El motor de avance encadena un `setTimeout` por bloque (arma uno para el bloque activo, y al disparar programa el siguiente) en vez de un intervalo único que recorra todos los bloques con offsets acumulados | Un único `setInterval`/reloj global que compare `Date.now()` contra los `inicio_segundos`/`fin_segundos` absolutos de cada bloque | Los offsets absolutos (`inicio_segundos`) están calculados a la velocidad base (1.0x) y acumulados desde el arranque del guion completo, no de la escena; usarlos tal cual para decidir "qué bloque toca ahora" habria exigido recalcular todos los offsets restantes de la escena cada vez que cambia la velocidad (requisito 2) o se avanza a mano (requisito 3), con el reloj global corriendo de fondo todo el tiempo. Con un temporizador por bloque, cada uno de esos dos casos es una llamada mas a `iniciarTemporizadorBloque()` sobre el bloque que corresponda, sin recalculo de nada mas: la duracion de un bloque siempre sale de restar sus dos marcas (T-12) y dividir por la velocidad vigente en el momento en que ESE bloque arranca, nunca antes. |
| 2026-09-02 | T-20 | La pausa (requisito 4) guarda `bloqueMsRestantes` como "milisegundos que faltan medidos desde la ultima marca" (`bloqueInicioMarca`), recalculado en cada pausa y usado tal cual en cada reanudacion, en vez de guardar una marca de tiempo absoluta de cuando deberia terminar el bloque (`bloqueFinPrevisto = Date.now() + duracionMs`) | Guardar el instante absoluto en que el bloque deberia terminar y, en cada pausa/reanudacion, comparar contra `Date.now()` | Las dos son equivalentes en un solo ciclo pausa-reanuda, pero la resta relativa es correcta sin condicion especial ante pausas/reanudaciones repetidas seguidas (el criterio de aceptacion no las prohibe): cada pausa resta el tiempo transcurrido desde la marca vigente, cada reanudacion pone una marca nueva y arma el temporizador con el resto tal cual, sin acumular una cadena de instantes absolutos que recalcular. Verificado a mano con Playwright (pausar, esperar, reanudar, esperar el resto exacto y comprobar que el bloque avanza en el momento esperado, ni antes ni tarde) sobre el guion real de `fixtures/reales/`. |
| 2026-09-02 | T-20 | `Configuracion` gana `paso_velocidad`/`velocidad_minima`/`velocidad_maxima` (nuevas constantes `PASO_VELOCIDAD` -- ya reservada desde T-18 sin usar --, `VELOCIDAD_MINIMA=0.5`, `VELOCIDAD_MAXIMA=2.0` en `config.py`), viajando al JSON incrustado junto al resto de datos del reproductor | Dejar los limites de velocidad como constantes escritas a mano dentro de `guion.js` | Regla dura de §0.2 ("sin numeros magicos": todo valor por defecto es sobreescribible por el dueno y documentado); `guion.js` no pasa por Python en ejecucion, asi que la unica via para que un limite de velocidad sea configurable por el dueno es que viaje dentro del JSON que ya genera `_construir_datos`, exactamente como ya hacen los colores/tipografia del reproductor (T-18) a traves de `estilo.css` -- aqui no aplica a una plantilla CSS/HTML por sustitucion de marcador, sino al propio JSON de datos, porque son valores que la LOGICA de `guion.js` necesita en tiempo de ejecucion, no solo su estilo. |
| 2026-09-02 | T-20 | El resaltado del bloque activo (`.bloque--activo` en `estilo.css`) es un tratamiento minimo (fondo y borde discretos) a proposito, sin atenuar el contexto anterior/posterior ni gradiente configurable | Implementar ya el resaltado completo que describe T-21 (contexto atenuado, contraste AAA) ya que el motor necesita *algun* indicador visible para verificarse | El propio enunciado de T-21 ("Resaltado, tipografia y tema de grabacion") es la tarea siguiente en el orden de §1 y tiene su propio criterio de aceptacion sobre atenuacion y contraste; adelantarlo aqui habria invadido su alcance y probablemente su trabajo se descartaria o duplicaria al llegar T-21. `.bloque--activo` es lo minimo que hace falta para que el motor sea verificable a ojo (via Playwright) sin decidir por adelantado el diseño visual que le corresponde a la tarea siguiente. |
| 2026-09-02 | T-21 | La atenuacion de contexto (requisito 1) se calcula en `guion.js` como opacidad por distancia (`Math.abs(indiceBloque - indiceActivo)`) aplicada directamente via `element.style.opacity`, indexando `datos.atenuacion_niveles` (gradiente configurable) con un suelo `datos.atenuacion_minima` mas alla del ultimo nivel, en vez de clases CSS fijas (`.bloque--atenuado-1`, `-2`...) | Generar en `estilo.css` una clase por nivel de atenuacion, con su opacidad fija en la hoja de estilos | El numero de niveles es configurable (`Configuracion.atenuacion_niveles`, una tupla de longitud libre): una clase CSS por nivel obligaria a la plantilla a generar tantas reglas como niveles trajera la configuracion del dueno, acoplando `estilo.css` a un valor de `config.py` que hoy son 3 elementos pero podria ser cualquier otro numero. Calcular la opacidad en JS a partir del array tal cual viaja en el JSON (mismo patron que `paso_velocidad`/`velocidad_minima`/`velocidad_maxima` de T-20) evita ese acoplamiento sin perder gradiente ni configurabilidad. Verificado con Playwright headless sobre el fixture real: con el bloque activo en el indice 2, los bloques 0/1/3 muestran opacidad 0.5/0.75/0.75 (niveles por defecto), exactamente los valores esperados por distancia. |
| 2026-09-02 | T-21 | El contraste AAA del bloque activo (requisito 3) se verifica con una funcion propia (`reproductor.contraste_relativo`, formula de luminancia relativa WCAG) y un test que falla si los colores por defecto bajan de razon 7:1, en vez de solo revisar a ojo con Playwright como hicieron T-19/T-20 para sus propios criterios visuales | Confiar solo en la revision visual manual del criterio de aceptacion, sin una comprobacion automatica de contraste | A diferencia de "se ve bien en la captura" (revisable solo por un humano), el contraste AAA es un numero objetivo con formula estandar (WCAG): calcularlo en Python permite que la regla se compruebe en cada `pytest`, no solo en la sesion que la escribio, y que un cambio futuro de `color_texto_reproductor`/`color_fondo_reproductor` (el dueno puede sobreescribirlos, son parte de `Configuracion`) se detecte automaticamente si rompe la razon minima -- mismo espiritu que otras reglas duras verificables por test (cobertura total, auto-contencion). No se anadio ademas una validacion en tiempo de generacion (avisar si el dueno configura colores que rompen AAA): los valores por defecto ya pasan con margen amplio (~18.5:1) y anadir esa validacion sin que ningun caso real la necesite hoy seria la misma abstraccion prematura que el proyecto ya evito en T-08/T-10/T-11; queda anotado aqui por si una sesion futura cambia los colores por defecto y el margen se estrecha. |
| 2026-09-02 | T-21 | El tamano de texto en vivo (requisito 2) es una unica preferencia global (`tamanoTextoActualPx` en `guion.js`, aplicada a `--tamano-base` via `documentElement.style.setProperty`), NO un array paralelo a `datos.escenas` como `velocidadesEscena` de T-20 | Que el tamano de texto tambien se recuerde por escena, igual que la velocidad | La velocidad es una propiedad del CONTENIDO de una escena (una escena densa puede necesitar mas tiempo); el tamano de letra es una propiedad de QUIEN LEE (su distancia a la camara, su vista), independiente de que escena este en pantalla -- cambiarlo una vez y que se resetee al pasar de escena habria sido una sorpresa desagradable en plena grabacion. Teclas elegidas, `[`/`]`: T-24 (proxima tarea que fija el mapa completo) reserva `M` (espejo, T-25), `H` (ocultar indicadores, T-23) y `?` (ayuda); `[`/`]` quedan libres y no chocan con ningun atajo ya anticipado por T-20 (`Espacio`, `+`/`-`, flechas, `PageUp`/`PageDown`, `R`). |
| 2026-09-02 | T-21 | El cursor se oculta tras inactividad SOLO si `document.fullscreenElement` es verdadero en el momento de disparar el temporizador, comprobado dentro de `ocultarCursor()` (no al programarlo) | Ocultar el cursor tras inactividad tambien en modo ventana (sin pantalla completa) | El requisito 4 lo pide explicitamente "en pantalla completa": en modo ventana (p. ej. pantalla completa denegada por el navegador de grabacion, caso ya contemplado desde T-19) el cursor no compite con ninguna imagen de camara, asi que ocultarlo ahi solo estorbaria a quien opera el reproductor con raton. Comprobar el estado en el momento de disparar (no de programar) cubre tambien el caso de salir de pantalla completa mientras el temporizador ya estaba corriendo, sin necesidad de cancelarlo aparte -- aunque se cancela igualmente en `fullscreenchange` para no dejar un temporizador fantasma corriendo de fondo. |
| 2026-09-02 | T-21 | El color de acento (`#f5c542`, usado en foco visible, indicador de pausa y borde del bloque activo) pasa a `Configuracion.color_acento_reproductor` -> `--color-acento`, cerrando una excepcion a "sin numeros magicos" que arrastraban T-19 y T-20 sin que ninguna de las dos lo tocara | Dejarlo como estaba (tres literales `#f5c542` en `estilo.css`) por no ser parte del alcance literal de ninguno de los requisitos de T-21 | T-21 es la tarea que reescribe el tema del reproductor de arriba a abajo (resaltado, colores, margenes); es la ocasion natural para cerrar esta excepcion sin abrir una tarea aparte solo para tres literales, y evita que una sesion futura que SI cambie `color_acento_reproductor` se encuentre con que el foco/pausa/borde activo no siguen el cambio. |
| 2026-09-02 | T-22 | El centrado del bloque activo mueve `window.scrollY` (el documento entero) con una interpolacion propia via `requestAnimationFrame`, cancelable en cada llamada nueva (`cancelAnimationFrame` + recalculo de `origen = window.scrollY` real), en vez de `elemento.scrollIntoView({behavior: 'smooth', block: 'center'})` | Usar el metodo nativo `scrollIntoView` con `behavior: 'smooth'` | `#app` no tiene `overflow` propio (crece con la altura natural de la pagina): no hay un contenedor con scroll interno que un `scrollIntoView` normal necesitaria, asi que mover `window.scrollY` ya es la via correcta con cualquiera de los dos metodos. Se descarta el nativo por el requisito 2 literal ("sin rebotes al avanzar rapido a mano"): con `scrollIntoView({behavior:'smooth'})`, dos llamadas seguidas antes de que la primera termine se encolan o se interrumpen sin control sobre el punto de partida real de la segunda, pudiendo producir un salto perceptible. La interpolacion manual recalcula `origen` como `window.scrollY` EN EL MOMENTO de cada llamada (la posicion real, este donde este tras cancelar cualquier animacion anterior), asi que un avance rapido a mano encadena animaciones sin rebote ni retroceso -- verificado con Playwright: cinco avances manuales en 20ms cada uno (mucho mas rapido que los 400ms de duracion configurada) terminan exactamente centrados, sin oscilar. |
| 2026-09-02 | T-22 | El requisito 3 ("si el texto cabe entero, no se desplaza nada") no se comprueba con una rama explicita ("¿cabe la escena en el viewport?"); sale gratis de acotar el objetivo entre `0` y `scrollMaximo()` (`scrollHeight - clientHeight`, nunca negativo) | Anadir una comprobacion explicita `if (scrollHeight <= clientHeight) return;` antes de calcular nada | Cuando el contenido cabe, `scrollMaximo()` es `0` por construccion, asi que el objetivo acotado siempre coincide con el origen (que ya esta en `0`, porque no hay margen para haberse desplazado antes) y la guarda existente `Math.abs(objetivo - origen) < 1` ya evita animar o saltar. Anadir una rama aparte para el mismo resultado habria sido logica duplicada sin necesidad -- el propio calculo ya es correcto en el caso limite, verificado con Playwright forzando un viewport alto (1400px) sobre la escena mas corta del guion real: `scrollY` permanece en `0`. |
| 2026-09-02 | T-22 | `centrarBloqueActivo` recibe un booleano explicito (`animado`) en cada punto de llamada, en vez de decidir por si sola si animar segun el contexto (p. ej. mirando si `escenaActual` acaba de cambiar) | Que la funcion detecte automaticamente cuando conviene animar | Los dos casos sin animacion (arranque de escena en `iniciarMotor`, redimensionado de ventana) y los tres con animacion (avance/retroceso manual, avance automatico, cambio de tamano de texto) no comparten ninguna senal en el estado global que una heuristica pudiera leer sin inventar una bandera aparte solo para esto -- es mas simple y mas legible que quien llama declare la intencion explicitamente, mismo criterio que ya uso T-20 al pasar un delta explicito a `ajustarVelocidad` en vez de inferirlo. |
| 2026-09-02 | T-22 | Bug real encontrado en esta sesion (no en el diseño, al verificar con Playwright): el foco diferido de `solicitarPantallaCompleta` (T-19, dentro del `.then()` de `requestFullscreen()`) disparaba el scroll-into-view por defecto del navegador al enfocar el boton "Volver al indice" (situado arriba de la pagina), deshaciendo el centrado que `iniciarMotor` acababa de calcular -- confirmado que en Chromium headless via Playwright la pantalla completa SI se concede, y el foco diferido llega DESPUES del centrado inicial. Se corrige anadiendo `{ preventScroll: true }` a las dos llamadas a `.focus()` de `solicitarPantallaCompleta` | (a) Retrasar la llamada a `centrarBloqueActivo` en `iniciarMotor` hasta que la promesa de pantalla completa se resuelva; (b) Eliminar el refoco de T-19 | (a) es fragil: la promesa puede resolverse, rechazarse (pantalla completa denegada) o no dispararse nunca (navegador sin `requestFullscreen`), y encadenar el centrado a un evento que no siempre ocurre dejaria escenas sin centrar en los casos donde la pantalla completa falla. (b) rompe el criterio de aceptacion de T-19 (recorrido por teclado con foco visible tras entrar en pantalla completa), una regresion real para arreglar un bug de T-22. `preventScroll: true` es la API estandar exacta para este conflicto (mover el foco sin mover el scroll) y no toca ningun otro comportamiento de T-19: el elemento se sigue enfocando, solo deja de arrastrar la pagina consigo. |
| 2026-09-02 | P-02 (hallazgo #9) | En `revalidacion.py`, cuando en la MISMA revalidacion coinciden una edicion manual sobre un bloque de origen (identidad `(indice_original, None)`) y la aceptacion de una particion de respiracion sobre ese mismo bloque, la particion NO se materializa esa pasada: `_materializar_marcados` recibe ahora el conjunto de indices con edicion previa a particion y se salta `_particion_aceptada_de` para ellos, y `_incidencias_conflicto_edicion_particion` deja constancia explicita en el informe de incidencias (nunca en silencio). La decision de aceptar la particion SI se persiste en `estado.reescrituras` -- no se revierte, solo se pospone su materializacion -- asi que una revalidacion posterior sin ese conflicto (el dueño no vuelve a tocar el bloque a mano) la aplica con normalidad | (a) Aplicar la particion y, si hay conflicto, sobrescribir la edicion manual con una de las dos mitades derivadas (perderia texto del dueño, exactamente el hallazgo); (b) Revertir automaticamente la decision de aceptar la particion al detectar el conflicto (marcarla RECHAZADA); (c) Fusionar heuristicamente el texto editado con la particion (p. ej. volver a trocear el texto editado en el punto de corte original) | (a) viola directamente el invariante (c) de §0.2, que es la razon de ser de este hallazgo. (b) es una decision de negocio que no le corresponde tomar al codigo sin que el dueño lo sepa: rechazar en su nombre una decision que SI tomo (aceptar la particion) seria tan silencioso como el bug original, solo que en la direccion opuesta. (c) reintroduce troceo automatico sobre texto que el dueño ya declaro autoritativo -- el propio invariante (c) dice que una edicion manual "jamas se sobrescribe", ni siquiera partiendola por su cuenta. Posponer sin tocar ninguna de las dos decisiones del dueño (el texto editado y la particion aceptada) y avisarlo con una incidencia explicita es la unica opcion que no decide nada en su nombre. **Limite conocido, no cerrado por esta P-XX:** en una revalidacion POSTERIOR sin tocar el bloque, si el dueño no vuelve a editar el ancla a mano, el emparejamiento ancla->identidad puede atribuir el texto editado completo a la mitad `'a'` de la particion (en vez de reconocerlo como "todavia sin particionar"), produciendo un bloque `'a'` con el texto integro y un bloque `'b'` con un fragmento del texto de ORIGEN sin editar -- verificado a mano reproduciendo el escenario. No es perdida de texto (el texto editado sigue integro, visible, en el bloque `'a'`) y el dueño lo veria de inmediato en el bloque `'b'` con texto extrano, pero es una forma de contenido incorrecto que merece un hallazgo de auditoria propio en la siguiente pasada en vez de ampliar el alcance de esta correccion urgente. |
| 2026-09-02 | T-23 | La cuenta atras (requisito 1) envuelve `iniciarMotor` en vez de vivir dentro de el: `reproducirEscena` llama a `iniciarCuentaAtras(function () { iniciarMotor(indice); })`, y con `cuenta_atras_activada=false` (o duracion 0) el callback se invoca al instante | Meter la cuenta atras como los primeros pasos de `iniciarMotor`, con una bandera interna que retrase el arranque del reloj del primer bloque | `iniciarMotor` ya establece el estado completo del motor (escena activa, bloque 0, cronometro, barra de progreso) y arrancarlo antes de que termine la cuenta atras habria significado o bien duplicar ese estado dos veces (uno "en pausa visual" durante la cuenta atras y otro real despues) o bien contaminar `iniciarMotor` con una fase de espera que no le corresponde. Una envoltura simple (callback) mantiene `iniciarMotor` como "arrancar ya", que es lo que el resto del codigo (T-20, T-22) ya asume al llamarlo, y hace trivial desactivar la cuenta atras sin ninguna rama dentro del motor. |
| 2026-09-02 | T-23 | El cronometro de la toma (requisito 2) recalcula el tiempo transcurrido siempre desde marcas de reloj absolutas (`cronometroInicioMarca` + `cronometroMsAcumulados`, ambas actualizadas con `Date.now()`), nunca acumulando por cada disparo del `setInterval` que solo redibuja la cifra cada 250 ms | Acumular el tiempo sumando el intervalo del `setInterval` en cada disparo (p. ej. `cronometroMsAcumulados += 250` en cada tick) | El criterio de aceptacion exige una deriva menor al 1% en una toma de 3 minutos; un `setInterval` real nunca dispara con una periodicidad exacta (el hilo de JS puede retrasarse por otro trabajo), asi que acumular por intervalo arrastraria ese error de planificacion indefinidamente. Recalcular desde `Date.now()` en cada lectura hace que la deriva sea estructuralmente nula (el valor mostrado es siempre la resta de dos marcas de reloj reales): el `setInterval` de 250 ms decide solo la frecuencia de redibujado, nunca el valor en si. Mismo patron que ya usa el reloj del bloque de T-20 (`bloqueInicioMarca`/`bloqueMsRestantes`), reutilizado aqui en vez de inventar uno nuevo. |
| 2026-09-02 | T-23 | La barra de progreso de la escena (requisito 3) se calcula por recuento de bloques (`(bloqueActual + 1) / total`), nunca como fraccion de `duracion_estimada_segundos` | Calcular el progreso como fraccion del tiempo estimado transcurrido, igual que el cronometro | El criterio de aceptacion es literal: "la barra llega al 100% justo con el ultimo bloque". Con progreso por tiempo, una toma mas lenta o mas rapida que la estimacion (deriva normal, no un fallo) dejaria la barra en un punto distinto de 100% justo cuando el locutor llega de verdad al ultimo bloque -- el caso comun, no el excepcional, dado que el ritmo real de quien graba rara vez coincide al segundo con el estimado. Progreso por bloques cumple el criterio por construccion, sin depender de que el tiempo real coincida con el calculado. |
| 2026-09-02 | T-23 | Los indicadores discretos (cabecera y barra de progreso) se ocultan con la tecla `H`/`h` alternando una unica clase (`indicadores-ocultos`) en `#vista-reproductor`, resuelta en CSS, en vez de ocultar cada indicador por separado desde JS | Alternar `hidden` en cada elemento individual (contador, cronometro, tamano, velocidad, pausa, barra de progreso) desde `alternarIndicadores` | Un unico interruptor de estado con la logica de que ocultar delegada al CSS es menos codigo y mas dificil de dejar incompleto si un futuro indicador se anade (T-24 documentara la tecla en el mapa completo, sin tocar este mecanismo). Ademas, al vivir el toggle en `classList` de un elemento que `renderizarReproductor` no recrea (solo vacia su `textContent`), el estado de "indicadores ocultos" sobrevive a un cambio de escena sin necesidad de guardarlo aparte -- util para quien graba varias escenas seguidas sin querer reactivar los indicadores en cada una. |
| 2026-09-02 | T-24 | El mapa de teclas del reproductor deja de comparar literales de tecla en el `switch` de `manejarTeclaReproductor` (patron de T-18 a T-23) y pasa a resolver `evento.key` contra una tabla `teclaAAccion` construida desde `datos.mapa_teclas`, que a su vez viene de `Configuracion.mapa_teclas_reproductor` (una tupla de pares `(accion, teclas)`, no un `dict`, para que el valor por defecto siga siendo inmutable en el dataclass congelado); `reproductor.py` lo convierte a `dict()` una sola vez al construir el JSON | (a) Mantener el `switch` literal de T-20/T-21 y anadir solo las teclas nuevas de T-24 (`Escape`, `?`) a mano; (b) Un `dict[str, tuple[str, ...]]` directamente en `Configuracion`, con `field(default_factory=...)` | (a) no habria cumplido el requisito 3 literal ("mapa de teclas configurable EN LA GENERACION"): remapear una tecla habria exigido tocar `guion.js`, que no pasa por Python en ejecucion. (b) es mutable (un dict es un objeto compartido, no un valor), lo que contradice el espiritu de `Configuracion` como dataclass `frozen=True` -- todos los demas campos compuestos (`atenuacion_niveles`, `pila_tipografica_reproductor`, `subordinantes`...) son tuplas por el mismo motivo. La tupla de pares conserva la inmutabilidad y el patron ya establecido; el `dict()` de conveniencia vive solo en el borde de serializacion (`_construir_datos`), donde ya no hay riesgo de mutacion compartida entre instancias de `Configuracion`. |
| 2026-09-02 | T-24 | `Espacio` pausa/reanuda por defecto (comportamiento heredado de T-20, sin cambios) pero puede avanzar el bloque en su lugar con `Configuracion.espacio_avanza_bloque=True` (requisito 1, "segun configuracion") | (a) Que `Espacio` siempre pause/reanude, sin la variante de avance; (b) Detectar automaticamente, por algun heuristico, si conviene pausar o avanzar | (a) no habria cumplido el requisito 1 literal, que pide explicitamente las dos variantes "segun configuracion". (b) es imposible de decidir bien: el navegador no expone que boton FISICO de un clicker Bluetooth envio la tecla (se identifica como un teclado corriente, sin metadatos de fabricante ni de boton), asi que no hay senal alguna con la que inferir la intencion -- es una decision que solo el dueno puede tomar, mirando su propio mando, no el codigo adivinando. |
| 2026-09-02 | T-24 | El antirrebote del clicker (`antirrebote_clicker_ms`, la constante `ANTIRREBOTE_CLICKER_MS` ya reservada en `config.py` desde T-20 sin usar) se aplica POR ACCION (`ultimaPulsacionPorAccion[accion]`), no con un unico cronometro global compartido por todas las teclas | Un solo temporizador global: cualquier tecla nueva se descarta si llega antes de `antirrebote_clicker_ms` desde la ultima tecla aceptada, sea cual sea | Con un cronometro global, pulsar rapido dos teclas DISTINTAS y legitimas (p. ej. `PageDown` seguido de `+` para subir la velocidad, un gesto real de quien opera el reproductor con las dos manos) se descartaria por error si llegan dentro de la misma ventana de 120 ms -- el requisito 2 solo pide tolerar el REBOTE DE CONTACTO de un clicker (la MISMA tecla disparada dos veces por un unico clic fisico), no limitar la velocidad general de tecleo. Por accion resuelve exactamente ese caso sin introducir el efecto secundario. |
| 2026-09-02 | T-24 | `evento.preventDefault()` se llama sobre toda tecla reconocida ANTES de aplicar el antirrebote, no despues | Aplicar el antirrebote primero y solo llamar a `preventDefault()` si la pulsacion se acepta | El requisito 2 pide explicitamente "evitar el desplazamiento nativo de la pagina"; si una pulsacion rechazada por antirrebote no cancelara la accion nativa, una repeticion rapida de `PageDown` seguiria desplazando la pagina aunque la accion del reproductor se ignorara -- el propio caso que el antirrebote existe para proteger (un clicker enviando la misma tecla dos veces) se veria con un salto de scroll visible pese a que el reproductor "no hizo nada". Cancelar antes de decidir si se acepta cubre ambos casos con una sola llamada. |
| 2026-09-02 | T-24 | `Esc` llama explicitamente a `salirPantallaCompleta()` (ya existente desde T-19) desde el nuevo mapa de acciones, en vez de confiar solo en que el navegador ya intercepta `Escape` para salir de pantalla completa por su cuenta | No anadir ningun manejador explicito para `Escape`, dejando que el comportamiento nativo del navegador (que ya ocurre, sin poder desactivarse con `preventDefault`) sea la unica implementacion del requisito 1 ("`Esc` sale de pantalla completa") | Sin un manejador explicito, el atajo no apareceria en `datos.mapa_teclas` ni, por tanto, en la ayuda `?` (requisito 3, "el mapa VIGENTE") -- quien consulte la ayuda durante una grabacion no veria como salir de pantalla completa, aunque la tecla funcione igual. Anadirlo al mapa configurable documenta un comportamiento que ya existia, sin cambiar su efecto: `salirPantallaCompleta()` ya comprueba `document.fullscreenElement` antes de actuar, asi que la llamada es inocua si el navegador ya salio por su cuenta. |
| 2026-09-02 | T-24 | La ayuda de teclado (`?`, requisito 3) construye su lista leyendo `datos.mapa_teclas` en tiempo de ejecucion (`construirListaAyudaTeclado`, recorriendo `Object.keys(...)`), en vez de una lista de `<li>` escrita a mano en el HTML generado | Escribir la lista de atajos directamente en la plantilla (server-side, en `reproductor.py`), con un `<li>` por accion conocida | El requisito 3 exige literalmente que la ayuda muestre "el mapa VIGENTE" -- si el dueno reconfigura `mapa_teclas_reproductor` (p. ej. quita una tecla o anade un sinonimo), una lista escrita en la plantilla se desincronizaria de silenciosamente del mapa real que usa `manejarTeclaReproductor`, mostrando atajos que ya no funcionan o callando otros que si. Leer siempre `datos.mapa_teclas` en tiempo de ejecucion hace que ambos (el manejador de teclado y su ayuda) sean, por construccion, la misma fuente de verdad. |
| 2026-09-02 | T-25 | El volteo horizontal (requisito 1) se implementa como `transform: scaleX(-1)` en CSS sobre `.escena` (titulo+bloques), no reescribiendo el texto ni volteando bloque a bloque; una segunda clase (`espejo-completo`, sobre `#vista-reproductor` entero) cubre el caso configurable en que el volteo debe incluir tambien los indicadores (`Configuracion.espejo_incluye_indicadores`, `False` por defecto) | (a) Voltear el texto en Python antes de incrustarlo (invertir caracteres o usar Unicode de ancho combinado); (b) Una unica clase que siempre voltee todo `#vista-reproductor`, sin la opcion de excluir los indicadores | (a) rompe la seleccion de texto, la busqueda del navegador y cualquier herramienta de accesibilidad, y complicaria sin necesidad el resto del pipeline (T-13, T-14, T-15 trabajan sobre el texto real, no sobre una version ya volteada); ademas `transform` es la tecnica estandar de los teleprompters de software reales. (b) no habria cumplido el requisito 1 literal ("sin afectar a la orientacion de los indicadores SI ASI SE CONFIGURA", que implica que debe ser configurable) -- los indicadores (cabecera, barra de progreso, cuenta atras, ayuda) son informacion para quien OPERA el reproductor mirando la pantalla directamente, no contenido que el cristal fisico deba reflejar, asi que voltearlos siempre habria estorbado al caso de uso mas comun. `scaleX(-1)` no afecta a la posicion/altura vertical de `getBoundingClientRect` (solo al eje horizontal), asi que el autoscroll de T-22 y el resaltado por opacidad de T-21 (requisito 2, "compatible con autoscroll, resaltado y cambio de tamano") funcionan sin ningun ajuste -- verificado con Playwright forzando un viewport reducido para provocar scroll real: la posicion vertical del bloque activo tras cada avance es identica con el espejo activo o no. |
| 2026-09-02 | T-25 | Persistencia minima adelantada de T-26, limitada al ajuste de espejo: `claveAlmacenamiento`/`leerPreferencia`/`guardarPreferencia` en `guion.js`, con `localStorage` envuelto en `try/catch` y clave `"teleprompter:" + datos.guion + ":espejo"` | (a) Dejar el ajuste solo en memoria, como T-19 dejo el estado de escena (`estadosEscena`), remitiendo la persistencia entera a T-26; (b) Disenar ya en esta tarea el mecanismo COMPLETO de T-26 (tamano de texto, velocidad por escena, ultima escena vista, indicadores) ya que hay que tocar `localStorage` de todos modos | (a) es el patron que T-19 sento a proposito (ver su fila en esta tabla, 2026-09-02) para NO adelantar el diseno de T-26 sin necesidad -- pero esa tarea no lo necesitaba en su propio criterio de aceptacion. La de T-25 SI lo exige literalmente ("el ajuste persiste tras recargar", ultima frase del criterio de aceptacion): dejarlo solo en memoria habria entregado T-25 incompleta a la espera de una tarea posterior (T-26) que ademas depende de ella en el orden de FASE B4. (b) habria invadido el alcance de T-26, que tiene su propio criterio de aceptacion (aviso si no persiste, exportar/importar, T-26 lo especifica aparte) y sus propias preferencias que T-25 no toca ni necesita. La solucion intermedia -- tres funciones pequenas y genericas, con la clave YA derivada del guion tal como pedira el requisito 2 de T-26 -- resuelve el requisito literal de T-25 sin decidir nada de T-26 en su nombre: esa tarea solo tiene que reutilizar `claveAlmacenamiento`/`leerPreferencia`/`guardarPreferencia` para el resto de preferencias, no disenarlas de nuevo. El hallazgo #5 de `auditoriacontinua.md` (localStorage no verificado contra `file://`) sigue abierto y aplica igual aqui: el `try/catch` evita el error, no garantiza la persistencia real en el navegador de grabacion. |
